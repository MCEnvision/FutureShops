# Phase 002 Execution Plan

> **Plan ID:** PLAN-PHASE-002
> **Phase ID:** CORE-PHASE-002
> **Owner:** Forge shop, transaction, persistence, and recovery owners
> **Classification:** MANDATORY
> **Master plan:** [plan.md](../plan.md)
> **Phase sequence:** 002 of 007

## Purpose and Ownership

This phase reproduces, repairs, integrates, and verifies Forge 1.20.1 issues 25, 32, and 34 without deleting affected data, weakening readiness, or losing or duplicating items or currency. It owns the detailed execution of `CORE-REQ-004`, `CORE-REQ-005`, and `CORE-REQ-006` only. The frozen master owns product scope, requirement assignment, support-line topology, owner decisions, external prerequisites, and the final completion endpoint.

The phase has three independently traceable defect streams joined by one Forge integration gate:

1. Issue 25 traces catalog parsing, compatible migration, durable stock activation, module readiness, authoritative offer publication, and buy, direct-sell, and bulk-sell visibility.
2. Issue 32 traces every FutureShops-owned player-state interaction, exact inventory proof, player-file durability barrier, transaction and claim recovery, restart, and reconnect behavior. The report is not a proven FutureShops root cause until `EXT-003` supplies an exact state or an equivalent deterministic reproduction.
3. Issue 34 compares finite- and infinite-stock money purchases at the first divergent authoritative boundary, then proves reservation, payment, delivery, claim, replay, persistence, and conservation behavior in controlled multiplayer.

`CORE-REQ-012`, `CORE-REQ-013`, and `CORE-REQ-014` are inherited persistence, integration, and economic contracts for these repairs. Their complete codebase-wide audits remain owned by later phases. This phase must not claim those later requirements complete.

## Evidence-Based Entry State

| Evidence class | Area | Finding | Source or command | Freshness condition |
|---|---|---|---|---|
| VERIFIED | Forge baseline | The authoritative support line starts from Forge 1.20.1 at `c6709e12ca7084ee068b2497a577b8d47c12f6fd`, Java 17, Forge 47.4.20, and Gradle 8.14.4. | `SRC-009`, master Section 1, and the `CORE-PHASE-000` baseline packet | Invalidated by a new approved `1.20.1` head or changed build metadata. Rebase the phase route onto the newly verified approved head before implementation. |
| VERIFIED | Phase ordering | `CORE-PHASE-000` provides the exact baseline, evidence requests, safe-fixture contract, and subsystem maps. `CORE-PHASE-001` must have passed its internal integration gate. | Master Section 13 and the Phase 000 and Phase 001 completion packets | Invalidated if either upstream packet is missing, points at a different repository, or leaves an internal integration failure unresolved. |
| VERIFIED | Issue 25 report | Existing-world server shop offers remain unavailable after update. A prior repair adopted an already-complete durable stock store only after verification, but the reporter still observed unavailable buying and inconsistent sell behavior. | `SRC-005`, issue 25 body and comments, including the report after commit `404c011` | Invalidated by new reporter evidence, changed issue state, or a later Forge revision that changes catalog, migration, readiness, or offer publication. |
| OBSERVED | Issue 25 implementation surface | Catalog definitions load through `ShopDefinitionLoader`; `ShopCatalog` publishes legacy or durable definitions; `CatalogStockRuntime` coordinates migration, reconciliation, activation, reload, and durable stock lookup. Offer execution reaches `ServerShopOfferService`, while direct and bulk sale paths consume the same authoritative catalog availability. | `SRC-011`; `ShopDefinitionLoader`, `ShopCatalog`, `CatalogStockRuntime`, `CatalogStockCutoverCoordinator`, and CodeGraph call maps | Invalidated by edits to these services, their callers, packet DTOs, configuration schema, or stock migration schema. |
| UNKNOWN | Issue 25 exact affected state | The preserved affected catalog, configuration, migration metadata, durable stock state, exact previous and failing artifacts, and complete startup-through-failure trace are not available at intake. | `EXT-002` | Remains unknown until sanitized evidence is preserved and hashed or a field-equivalent deterministic fixture is justified. |
| VERIFIED | Issue 32 report | A dedicated-server player reported unusable player data twice and deleted the file to recover. The triggering operation, exact loader version, affected file, matching FutureShops state, and exact invalid field remain unknown. | `SRC-006` and issue 32 body and comments | Invalidated by new preserved evidence or a deterministic reproduction that narrows the fault. |
| OBSERVED | Issue 32 implementation surface | FutureShops forces player data durability after inventory mutation through `PlayerDataDurabilityBarrier`; player inventory receipts, exact item custody and delivery, claims, transaction state, and recovery records span player files and FutureShops-owned SavedData. | `SRC-002`, `SRC-011`; `PlayerDataDurabilityBarrier`, player inventory receipt and access components, `ClaimSavedData`, transaction SavedData, and escrow runtime call maps | Invalidated by changes to inventory mutation, receipt, save, claim, transaction, or recovery code. This is a surface map, not proof of causation. |
| UNKNOWN | Issue 32 root cause | No exact invalid player state or deterministic equivalent exists at intake. No implementation change may be represented as the issue 32 fix without reproducing the FutureShops-owned cause. | `EXT-003` | Resolved only by preserved sanitized state or a deterministic fixture meeting the master prerequisite. |
| OBSERVED | Issue 34 report | On a dedicated multiplayer server, finite-stock money purchases fail while the same offer succeeds when stock is infinite, and current logs provide no actionable FutureShops diagnostic. | `SRC-008` and issue 34 body and comments | Invalidated by exact environment evidence, a reproduced fault, or a later transaction-path revision. |
| OBSERVED | Issue 34 implementation surface | `ShopBuyService` prepares server-shop purchases and delegates to `ServerShopPurchaseService`; finite stock reaches persistent stock reservation and mutation, while payment, transaction journal, item delivery, claims, replay receipts, and persistence are owned by escrow runtime services. | `SRC-002`, `SRC-011`; `ShopBuyService`, `ServerShopPurchaseService`, `PersistentStockRepository`, `StockSavedData`, purchase commit and replay components | Invalidated by edits to quote, stock, purchase, payment, claim, replay, or result packet paths. |
| UNKNOWN | Controlled multiplayer | The isolated dedicated server with two independent clients and complete conservation instrumentation is not confirmed available. | `EXT-004` | Resolved only by a recorded environment that meets the master prerequisite. |
| OBSERVED | Existing verification | Focused catalog, stock migration, player durability, purchase, replay, journal, claim, and conservation tests exist, including `CatalogStockMigratorTest`, `PlayerDataDurabilityBarrierTest`, `ServerShopPurchaseServiceTest`, `PersistentStockRepositoryTest`, and `EscrowCrossDomainConservationAuditTest`. | `SRC-011` and repository test inventory | Invalidated by any relevant source or fixture change or by a test that is skipped, flaky, or not executed at the candidate revision. |

No `UNKNOWN`, `OBSERVED`, or `PROPOSED` finding becomes `VERIFIED` without the exact source revision, fixture hash, environment, procedure, expected result, actual result, and decisive evidence.

## Scope Boundaries

### Included Scope

- `CORE-REQ-004`: reproduce the issue 25 unavailable-offer state beyond the first migration repair, fix the authoritative cause, preserve valid catalog and stock data, provide actionable invalid-entry or readiness diagnostics, verify startup, restart, and reload, and retain `EXT-002` reporter acceptance as mandatory closure evidence.
- `CORE-REQ-005`: preserve and inspect issue 32 evidence, deterministically prove the FutureShops-owned invalid state, repair only the responsible FutureShops boundary, maintain backward-compatible reads, preserve unrelated player and modded inventory data, and verify restart, reconnect, repeated transaction, and recovery without deletion.
- `CORE-REQ-006`: reproduce the finite-stock-only money purchase failure, repair the authoritative stock and transaction path, add bounded diagnostics, and prove single charge, single delivery or durable claim, single stock decrement, replay safety, persistence, and conservation.
- The portions of `CORE-REQ-009`, `CORE-REQ-012`, `CORE-REQ-013`, `CORE-REQ-014`, `CORE-REQ-015`, `CORE-REQ-016`, `CORE-REQ-017`, and `CORE-REQ-020` directly required to file newly verified defects, protect phase-owned state, verify these repairs, isolate the Forge line, update affected documentation, and keep blocked issues open. Their global completion remains in later phases.

### Explicit Exclusions

- `CORE-REQ-003` and issue 22 are owned by `CORE-PHASE-001`. No NeoForge source, build metadata, or `1.21.1` integration belongs in this phase.
- `CORE-REQ-007` and `CORE-REQ-008`, including issue 33 bulk listing creation and conflict behavior, are owned by `CORE-PHASE-003`.
- The complete security and command audit belongs to `CORE-PHASE-004`; the complete persistence and conservation audit belongs to `CORE-PHASE-005`; the complete backend integration audit belongs to `CORE-PHASE-006`; final candidate, repeated audit, and publication-readiness work belongs to `CORE-PHASE-007`.
- `FUT-001` through `FUT-005` and `NG-001` through `NG-008` remain excluded. In particular, this phase does not publish, declare stable status, change pinned platform versions, delete state, bypass readiness, or merge loader lines.
- A speculative hardening change is not an issue 32 repair. If no exact cause is reproduced, record the evidence gap and leave issue 32 and `CORE-REQ-005` open.
- Unrelated dependency pull request 28 is not part of the phase unless the frozen master is revised or exact branch health proves it is a required prerequisite.

## Phase Contract

### CORE-PHASE-002 — Forge Defect Reproduction, Repair, and Conservation Proof

**Objective:** Reproduce and repair every evidence-backed root cause for Forge issues 25, 32, and 34 on the correct Forge line, merge the verified changes into `1.20.1`, and retain precise external blockers without destructive recovery or value loss.
**Owner:** Forge defect integration
**Dependencies:** CORE-PHASE-000, CORE-PHASE-001, CORE-REQ-002, CORE-REQ-016, DEC-002, DEC-004, DEC-005, EXT-002, EXT-003, EXT-004
**Canonical requirements:** CORE-REQ-004, CORE-REQ-005, CORE-REQ-006
**Documentation and release impact:** Update only documentation and issue records affected by verified behavior, diagnostics, migration, recovery, or operator procedures. Preserve Forge candidate identity `3.0.0-beta.2`; do not publish or create a release.
**Next transition:** CORE-PHASE-003

**Entry criteria**

- The Phase 000 completion packet identifies the latest approved `1.20.1` head, exact toolchain, issue packets, sanitized evidence requests, safe fixture locations, subsystem maps, and baseline test results.
- The Phase 001 internal integration gate has passed. Any missing `EXT-001` reporter acceptance is carried explicitly and does not contaminate the Forge branch.
- The Forge work branch derives from the latest approved `origin/1.20.1` head. The branch, base commit, dirty state, and unrelated user changes are recorded before editing.
- Issues 25, 32, and 34 remain canonical and have no unclassified duplicate. Their current comments, attachments, linked changes, and evidence gaps are preserved.
- Evidence handling uses isolated copies. Original worlds, player files, catalogs, journals, checkpoints, ledgers, custody, claims, and logs are read-only and retained with hashes.
- Baseline focused tests and `bash ./gradlew test` and `bash ./gradlew build` results are recorded. Existing failures are classified before attributing any later result to a repair.

**Implementation scope**

- CORE-REQ-004, CORE-REQ-005, and CORE-REQ-006 define the complete mandatory implementation boundary detailed below.

**Detailed implementation scope**

- Trace issue 25 from on-disk JSON and schema validation through migration state, durable stock reconciliation, escrow readiness, catalog publication, offer snapshots, and buy and sell consumers. Repair the first authoritative divergence and retain last-known-good state on invalid reload or migration failure.
- Trace issue 32 from packet or command entry through item and currency mutation, player inventory receipt, player-file save and force, transaction journal, claim delivery, lifecycle events, restart, reconnect, and recovery. Repair only a reproduced FutureShops-owned fault and preserve unrelated player NBT byte-for-byte or semantically where platform serialization legitimately normalizes ordering.
- Trace issue 34 from quote and request identity through finite stock reservation, payment custody, commit, delivery, claim or compensation, stock persistence, replay receipt, result packet, and restart. Compare the same fixture with infinite stock to isolate the earliest divergence.
- Add focused regressions before repair where feasible, actionable bounded diagnostics, compatibility fixtures for any changed representation, and exact-revision runtime evidence.

**Execution order**

- `P002-TASK-001` through `P002-TASK-012` execute the CORE-PHASE-002 task sequence in order.

**Detailed task sequence**

1. `P002-TASK-001` freezes the Forge base revision, issue packets, evidence custody, external-blocker state, and phase-owned subsystem maps.
2. `P002-TASK-002` creates deterministic, disposable fixture families and before-state conservation manifests for all three issues.
3. `P002-TASK-003` reproduces issue 25 and isolates the first catalog, migration, readiness, publication, or consumer divergence.
4. `P002-TASK-004` repairs and regression-tests the verified issue 25 root cause, including migration and last-known-good behavior.
5. `P002-TASK-005` inventories every issue 32 player-state write, ownership transition, durability barrier, and recovery path without mutating preserved evidence.
6. `P002-TASK-006` reproduces the exact issue 32 invalid state or records `EXT-003` as unresolved; only a proven FutureShops-owned cause proceeds to repair and recovery testing.
7. `P002-TASK-007` reproduces issue 34 with paired finite- and infinite-stock fixtures and captures the earliest authoritative divergence.
8. `P002-TASK-008` repairs issue 34 and proves stock, money, delivery, claim, replay, persistence, diagnostics, and concurrency invariants.
9. `P002-TASK-009` runs cross-defect integration and fault matrices so one repair cannot mask or invalidate another.
10. `P002-TASK-010` reconciles affected documentation, issue records, migration guidance, and recovery instructions with verified behavior.
11. `P002-TASK-011` executes the full Forge verification ladder, inspects the JAR and complete diff, and assembles exact evidence.
12. `P002-TASK-012` obtains review, integrates the phase through a Forge pull request, verifies `origin/1.20.1`, and emits the completion and blocker packet for `CORE-PHASE-003`.

Tasks 3 and 5 may proceed in parallel after Task 2 because they use isolated fixtures and different primary ownership boundaries. Task 7 may also proceed in parallel if `EXT-004` is available. Repairs that touch shared escrow, stock, catalog, player inventory, or recovery code serialize behind a single change-impact review. Tasks 9 through 12 remain ordered after all implemented repairs.

**Required evidence**

- Exact before-and-after issue 25 trace across catalog parse, migration, stock authority, escrow readiness, snapshot publication, buy, direct sell, bulk sell, startup, restart, and reload.
- `EXT-003` field-level issue 32 proof or a deterministic equivalent, including original and working-copy hashes, causal operation, exact invalid field or ownership mismatch, recovery result, and proof that unrelated player data is unchanged.
- `EXT-004` paired finite- and infinite-stock multiplayer trace with stable request identities, balances, inventories, stock, escrow, claims, journal and persisted state, retries, disconnect, restart, and reconnect.
- Focused unit, integration, migration, replay, concurrency, recovery, and conservation regressions at the exact changed revision.
- Complete Forge test, applicable data and GameTest, build, server, client, multiplayer, restart, reconnect, JAR, and diff evidence in repository verification order.
- Merge, required-check, review, and exact `origin/1.20.1` ancestry proof. `EXT-002`, `EXT-003`, or `EXT-004` that remains unavailable is recorded as an endpoint blocker rather than passed.

**Exit criteria**

- Each reproducible issue 25, 32, or 34 root cause has a failing-before and passing-after regression where feasible, a verified repair at the authoritative boundary, and no unresolved source-controlled phase-owned defect.
- All implemented repairs are merged through the Forge phase pull request, required checks pass, and `origin/1.20.1` contains the merge commit. No direct integration push, cross-loader change, or unsupported version change occurred.
- Issue 25 valid offers survive update, startup, restart, and reload; invalid data fails closed with exact bounded diagnostics. `EXT-002` reporter acceptance is linked if available, otherwise issue 25 and `CORE-REQ-004` remain externally blocked and open.
- Issue 32 recovery loads the exact preserved or deterministically reproduced state without deletion, preserves unrelated data, and survives restart, reconnect, repeated transactions, and recovery without duplication or loss. If `EXT-003` is unavailable and no exact deterministic reproduction exists, no speculative fix is claimed and the requirement remains externally blocked.
- Issue 34 finite-stock purchases charge once, deliver once or create one durable claim, decrement stock once, and remain replay-safe and persistent across conflict, failure, disconnect, retry, restart, and reconnect. `EXT-004` evidence is linked if available, otherwise the real multiplayer gate remains an explicit final blocker.
- Documentation and issue state describe only merged verified behavior. Blocked issues remain open with the exact missing evidence.
- The internal integration gate may pass with approved external blockers only when all independent source-controlled work, deterministic evidence, merge gates, and downstream compatibility checks pass, and `CORE-PHASE-003` does not consume the missing evidence.
- No known mandatory phase-owned defect remains. The final phase, not this phase, verifies the owner-selected completion endpoint and plan-wide Definition of Done.

## Inputs and Upstream Contracts

| Input or contract | Provider | Required state | Validation | Failure behavior |
|---|---|---|---|---|
| Exact Forge baseline packet | `CORE-PHASE-000` | Current approved `origin/1.20.1` head, Forge toolchain, version, dirty-state record, focused and full baseline results | Compare commit, branch ancestry, build metadata, Java version, and retained command outputs | Stop branch creation or repair until the baseline packet is reconciled. Do not guess a base revision. |
| Canonical issue packets | `CORE-PHASE-000`, `SRC-005`, `SRC-006`, `SRC-008` | One issue per behavior with environment, reproduction, evidence gaps, acceptance, and duplicate search | Re-read issue bodies, comments, linked changes, labels, and attachments at phase start | Preserve changed evidence, update the packet outside the protected plan, and reclassify tasks before edits. |
| Phase 001 internal integration result | `CORE-PHASE-001` | NeoForge source-controlled work is integrated or explicitly not required; any external acceptance blocker is isolated | Inspect Phase 001 completion packet and branch proof | Do not start the dependent Forge phase if internal integration failed. Never copy NeoForge changes into Forge. |
| Safe fixture and evidence custody contract | `CORE-PHASE-000`, `DEC-004`, `SRC-013` | Originals are immutable, working copies are isolated, sanitized, access-bounded, and hashed | Verify read-only original, copy manifest, hashes, redaction log, and restore procedure | Stop mutation tests. Preserve evidence and create a new verified working copy. |
| Issue 25 affected-world evidence | `EXT-002` | Catalogs, configs, versions, logs, migration and stock state, and preserved world copy are exact and sanitized | Hash files, inventory versions, reproduce on an isolated copy, compare with issue report | If unavailable, request exact evidence and use only a justified equivalent fixture. Keep reporter acceptance open. |
| Issue 32 exact-state proof | `EXT-003` | Preserved player state plus matching FutureShops world context, or deterministic field-equivalent fixture | Validate hashes, schema versions, player identity redaction, causal operation, and repeated reproduction | Do not infer causation or claim repair. Continue only read-only mapping and independently justified safety tests. |
| Controlled Forge multiplayer environment | `EXT-004` | Isolated dedicated server, exact revision, at least two independent clients, finite and infinite fixtures, capture capability | Record Java, Forge, mod list, configs, artifact hash, player roles, clean backup, and capture procedure | Keep real multiplayer evidence blocked. Deterministic tests may proceed but do not substitute for the prerequisite. |
| Catalog and stock contracts | `SRC-002`, `ShopDefinitionLoader`, `ShopCatalog`, durable stock migration and repository components | Complete snapshot validation, stable listing identities, repeat-safe migration, last-known-good reload, durable authority after cutover | Catalog fixture tests, migration metadata inspection, stock checksum and reservation reconciliation | Fail closed, preserve old catalog and stock state, emit exact path and field or readiness reason, and restore the complete matching copy. |
| Player and recovery contracts | `SRC-002`, player inventory receipt and durability components, claims and escrow runtime | Only FutureShops-owned state changes, inventory proofs are exact and bounded, receipts and claims remain recoverable | Field-level before-and-after diff, save and force results, transaction and claim lineage | Stop mutation, keep receipts unconfirmed, retain claims and journals, preserve the player file, and recover from the complete matching copy. |
| Transaction and conservation contracts | `SRC-002`, `CORE-REQ-014` | Stable request UUID, checked integer minor units, authoritative server state, prepare and custody before commit, one delivery or durable claim | Request-level ledger, stock, money, inventory, custody, claim, and journal reconciliation | Freeze unsafe mutation, preserve lineage, compensate once or retain recoverable custody, and never retry with a new logical request identity. |

## Outputs and Downstream Contracts

| Output or contract | Consumer | Guaranteed state | Compatibility or versioning | Evidence |
|---|---|---|---|---|
| Merged Forge phase revision | `CORE-PHASE-003` through `CORE-PHASE-007` | All source-controlled issue 25, 32, and 34 repairs are integrated into `1.20.1`; external blockers are explicit | Forge 1.20.1, Java 17, Forge 47.4.20, target candidate `3.0.0-beta.2`; no NeoForge contamination | PR, checks, review, merge commit, ancestry, metadata, and diff packet |
| Catalog availability contract | `CORE-PHASE-003`, `CORE-PHASE-005`, `CORE-PHASE-006` | Valid catalogs publish one authoritative available snapshot after readiness; invalid reloads retain last-known-good state | Existing schema and listing identities remain readable; any migration is versioned and repeat-safe | Issue 25 regressions, fixture diffs, startup, restart, reload, server and client traces |
| Player-state ownership map and proof | `CORE-PHASE-005` | Every phase-touched player mutation, receipt, save barrier, claim, and recovery handoff is mapped; exact cause and recovery evidence are linked or `EXT-003` remains open | Compatible old state reads remain supported; unrelated player fields and modded inventory remain untouched | Field-level diffs, fixture hashes, lifecycle and recovery results |
| Finite-stock transaction contract | `CORE-PHASE-005`, `CORE-PHASE-006` | Finite stock shares the authoritative purchase flow and adds exactly one durable reservation and decrement; every terminal result conserves value | Stable request, listing, stock, transaction, claim, and replay identities | Issue 34 unit, concurrency, fault, multiplayer, restart, reconnect, and conservation packet |
| Diagnostic contract | Operators, later audits, issue reporters | One bounded actionable failure record identifies stable request, listing, stock, transaction, lifecycle stage, safe recovery state, and correlation code without private NBT or secrets | Existing client and protocol compatibility remains stable unless a versioned change is required | Negative tests, log inspection, localized result checks, documentation |
| External blocker register | `CORE-PHASE-007` | `EXT-002`, `EXT-003`, and `EXT-004` each show available, passed, failed, or unavailable with exact next action | Blockers never masquerade as passed requirements and survive phase transitions | Issue links, evidence requests, acceptance links, and completion packet |
| Phase verification packet | `CORE-PHASE-003` and final verification | Exact revision, commands, environments, results, fixtures, hashes, failures, invalidation scope, and open blockers are reproducible | Any later relevant change invalidates named evidence and triggers rerun | Retained logs, reports, JAR inspection, diff inspection, and GitHub links |

## Work Packages

| Task ID | Requirement IDs | Work | Inputs and dependencies | Outputs | Affected components or interfaces | Verification |
|---|---|---|---|---|---|---|
| `P002-TASK-001` | `CORE-REQ-004`, `CORE-REQ-005`, `CORE-REQ-006` | Freeze the exact Forge base, refresh issue packets and duplicate searches, classify external prerequisites, establish immutable evidence custody, and confirm subsystem maps and test inventory. | Phase 000 packet, Phase 001 internal gate, `SRC-005`, `SRC-006`, `SRC-008`, `SRC-009`, `SRC-011`, `SRC-013` | Phase execution manifest with base commit, issue state, evidence hashes, blocker state, ownership map, and existing failures | Git and GitHub evidence, build metadata, catalog, player-state, transaction, test, and documentation surfaces | Read-only ancestry and issue checks, evidence hashes, Java and Gradle identity, baseline focused tests, `test`, and `build` results |
| `P002-TASK-002` | `CORE-REQ-004`, `CORE-REQ-005`, `CORE-REQ-006` | Build disposable fixture families and a common conservation manifest. Include current and legacy catalogs, migration stages, player and FutureShops state pairs, finite and infinite stock, balances, inventories, claims, and stable request identities. | `P002-TASK-001`, safe fixture contract, `EXT-002` through `EXT-004` when available | Reproducible fixture manifests with provenance, schema, hashes, expected state, reset procedure, and redaction record | Test resources and harnesses only; no production or original world mutation | Recreate fixtures from clean copy, compare hashes and canonical state, and prove teardown restores the clean baseline |
| `P002-TASK-003` | `CORE-REQ-004` | Reproduce issue 25 across update, first startup, readiness convergence, catalog publication, direct buy, direct sell, bulk-sell destination lookup, restart, and explicit reload. Trace the first divergence after the prior `404c011` repair. | `P002-TASK-002`, `EXT-002` if available, catalog and migration ownership map | Failing regression and causal trace or exact evidence-gap record | `ShopDefinitionLoader`, `ShopCatalog`, `CatalogStockRuntime`, migration SavedData and coordinator, stock repository, escrow readiness, offer service, snapshots, relevant packets and sell consumers | Before trace fails with the reported stable result; control fixture succeeds; every lifecycle stage records exact authoritative state without private data |
| `P002-TASK-004` | `CORE-REQ-004` | Repair the verified issue 25 cause at the first authoritative boundary. Preserve compatible catalogs and durable stock, make migration repeat-safe, retain last-known-good snapshots, and report invalid path, entry, stage, checksum, or readiness cause without hiding valid unrelated offers. | `P002-TASK-003` and the master persistence, recovery, and integration invariants | Minimal repair, migration compatibility table if needed, diagnostics, and focused regression suite | Same issue 25 surface plus documentation and localization when behavior changes | Catalog parse, migration, cutover, readiness, reload, snapshot, buy, direct sell, and bulk sell tests; startup, restart, reload, server and client smoke; `EXT-002` acceptance when available |
| `P002-TASK-005` | `CORE-REQ-005` | Inventory every FutureShops path that can affect a player file, inventory, persistent tag, capability-adjacent state, item stack NBT, currency, receipt, custody, claim, or login recovery. Mark state owner, thread, prepare point, write point, durability point, recovery owner, and unrelated-data boundary. | `P002-TASK-001`, `SRC-002`, `SRC-006`, `SRC-011`, `SRC-013` | Issue 32 state-ownership and mutation matrix with candidate causal seams and non-mutating inspection procedure | Player inventory access, receipt store, `PlayerDataDurabilityBarrier`, transaction and claim SavedData, physical currency, escrow recovery, player login and logout hooks | Source and call-path review reaches every writer and recovery caller; matrix has no unowned mutation or unexplained cross-file commit boundary |
| `P002-TASK-006` | `CORE-REQ-005` | Use `EXT-003` or a deterministic equivalent to reproduce the invalid state, minimize the causal operation, add failing serialization and lifecycle regressions, repair only the FutureShops-owned cause, and prove compatible non-destructive recovery. | `P002-TASK-002`, `P002-TASK-005`, `EXT-003`, `CORE-REQ-014` | Exact invalid-field proof and repair, or an explicit blocked record with no claimed fix | Proven player-state, inventory proof, receipt, claim, transaction, save, lifecycle, or recovery boundary only | Sanitized field-level diff, serialization and exact-item tests, failure injection before and after each durability boundary, repeated restart and reconnect, unrelated modded inventory preservation, no deletion |
| `P002-TASK-007` | `CORE-REQ-006` | Reproduce issue 34 with the same shop, item, price, player, balance, and inventory under finite and infinite stock. Capture quote, packet, reservation, payment, journal, commit, delivery, claim, replay, and persistence state to locate the earliest divergence. | `P002-TASK-002`, `EXT-004` when available, `SRC-008` | Failing finite-stock regression, successful infinite control, and causal transaction trace | `ShopBuyService`, `ServerShopPurchaseService`, stock repository and SavedData, purchase commit and replay, escrow runtime, economy provider, claim delivery, result packet | Deterministic service test plus dedicated multiplayer trace; failure occurs at one identified boundary before unsafe mutation or with fully reconciled recovery |
| `P002-TASK-008` | `CORE-REQ-006` | Repair the verified finite-stock cause, align finite and infinite purchase contracts, and add bounded server and client diagnostics. Cover insufficient funds, stale stock, concurrent buyers, full inventory, provider failure, disconnect, retry, restart, and reconnect. | `P002-TASK-007`, `CORE-REQ-012`, `CORE-REQ-013`, `CORE-REQ-014`, `EXT-004` | Minimal transaction repair, stable result mapping, diagnostics, and complete conservation regressions | Purchase preparation and service, stock reservation and mutation, payment, journal, custody, claims, replay, persistence, networking | `ServerShopPurchaseServiceTest`, stock math and repository tests, purchase commit and replay tests, fault and concurrency tests, multiplayer matrix, exact conservation report |
| `P002-TASK-009` | `CORE-REQ-004`, `CORE-REQ-005`, `CORE-REQ-006` | Integrate all verified repairs and run cross-defect success, failure, lifecycle, and recovery matrices. Confirm catalog readiness cannot expose unsafe stock, player recovery cannot double-apply purchase state, and transaction faults cannot corrupt catalog or player evidence. | Tasks 4, 6, and 8; task 6 may be a documented external block | One coherent Forge change set and invalidation graph | Shared catalog, stock, escrow, player inventory, claim, persistence, network, and lifecycle interfaces | Combined focused tests, restart and reconnect loops, recovery from every injected boundary, cross-domain conservation audit, complete fixture diff |
| `P002-TASK-010` | `CORE-REQ-004`, `CORE-REQ-005`, `CORE-REQ-006` | Update user, maintainer, migration, diagnostic, backup, recovery, compatibility, and issue records for verified changes only. Record exact external blockers and reporter instructions. | `P002-TASK-009`, verified behavior and schemas | Accurate tracked documentation and synchronized issue evidence | `README.md`, `DOCUMENTATION.md`, `docs/README.md`, `docs/backup-restore.md`, `docs/compatibility-matrix.md`, `docs/community-bug-regression-test-gaps.md`, and only other directly affected guides | Source-to-document review, link checks, literal command and path checks, recovery rehearsal, issue text review under repository writing rules |
| `P002-TASK-011` | `CORE-REQ-004`, `CORE-REQ-005`, `CORE-REQ-006` | Run the full Forge verification ladder at one frozen revision and inspect packaged output and the complete diff. Retain failures and rerun invalidated downstream checks after any correction. | `P002-TASK-009`, `P002-TASK-010` | Exact candidate verification packet and review-ready commit | Complete Forge source, tests, resources, docs, JAR, and worktree | Focused tests, `test`, applicable `runData`, applicable `runGameTestServer`, `build`, `runServer`, `runClient`, multiplayer, restart, reconnect, JAR and diff inspection |
| `P002-TASK-012` | `CORE-REQ-004`, `CORE-REQ-005`, `CORE-REQ-006` | Commit and push verified work on the phase branch, open or update the authorized phase pull request, obtain one private independent review when available, resolve findings, wait for required checks, merge through GitHub, verify `origin/1.20.1`, and publish the internal completion packet. | `P002-TASK-011`, GitHub access and signing gates, repository phase workflow | Merged Forge revision, green checks, review record, retained external blockers, and exact Phase 003 handoff | Git branch, pull request, checks, issue and project state, completion evidence | Signed commit proof, PR merge proof, remote ancestry, no direct support-branch push, issue states agree with evidence, completion packet is revision-exact |

## Architecture and Implementation Boundaries

### State Ownership and Dependency Direction

| State or transition | Authoritative owner | Readers and projections | Persistence boundary | Phase rule |
|---|---|---|---|---|
| Shop JSON and parsed definitions | `ShopDefinitionLoader` validates files; `ShopCatalog` owns the published in-memory snapshot | Server offer services, sell routing, client snapshots, admin diagnostics | `config/futureshops/shops/` with complete candidate validation and atomic writer paths | Invalid input cannot replace the last valid snapshot or make unrelated valid offers disappear without an exact failure classification. |
| Catalog stock authority and migration | Catalog stock migration components and durable stock repository on the logical server | Catalog display, purchase reservation, admin stock operations | Migration SavedData, `StockSavedData`, checkpoints, and journal-backed mutation | Legacy-to-durable activation is repeat-safe. Existing nonempty durable state is adopted only through full identity, quantity, reservation, receipt, fingerprint, and conservation verification. |
| Module and escrow readiness | Escrow runtime and server lifecycle services | Network capability snapshots and client navigation | Runtime SavedData, recovery lineage, and authoritative capability projection | UI and packet handlers may report unavailability but never manufacture readiness. Claims remain reachable during recovery or disabled module states. |
| Purchase request identity | Server request boundary and transaction service | Replay lookup, result packets, audit diagnostics | Stable request UUID in intent, journal, transaction, replay, stock, and claim records | A logical retry reuses its request identity. Conflicting fingerprints fail closed before new value mutation. |
| Finite stock | Durable stock repository and stock mutation service | Quote and client display are projections | Stock reservation and mutation records plus SavedData and journal lineage | Reserve before commit, decrement once, release or reconcile on failure, and never trust client stock. Infinite stock is a policy branch, not a separate payment authority. |
| Money and payment | Authoritative economy provider adapter and escrow payment service | Client balance and history snapshots | Escrow transaction, ledger, provider receipt, journal, claim or compensation | Use checked integer minor units. Charge once only after validated preparation. Provider uncertainty leaves recoverable custody or a stable retry state. |
| Item delivery and player inventory | Server player inventory gateway under escrow custody | Client inventory is presentation only | Player data file, inventory receipt, exact item custody, transaction and claim records | Exact item state is proven before and after mutation. Delivery failure creates or retains one durable claim. Unrelated slots and player fields are outside FutureShops ownership. |
| Player-file durability | Minecraft player save plus `PlayerDataDurabilityBarrier` and receipt protocol | Recovery and evidence inspection | Final player file and parent-directory durability result | A failed or unsupported force result is explicit. An unconfirmed receipt is never treated as durable completion. No repair edits or deletes the original player file. |
| Claims and recovery | Escrow claim, custody, journal, checkpoint, ledger, and recovery services | Claim UI and admin diagnostics | One versioned recovery lineage across SavedData and journal files | Claims do not expire and remain accessible. Partial failure must converge to exactly one delivery, compensation, or durable claim. |
| Diagnostics | Authoritative boundary that detects the failure | Localized client response, bounded server log, operator status | Logs and issue evidence, not product truth | Include stable codes and correlation identities but exclude credentials, raw private player data, sensitive NBT, and exploit-enabling details. |

### Transaction Boundary

For issue 34 and any issue 32 path involving value movement, the required order is validate request and permissions, resolve authoritative listing and price, verify readiness, construct stable identity and fingerprint, reserve finite stock when applicable, prepare payment and item custody, durably record intent, commit authoritative state, attempt delivery, create or retain a durable claim on delivery failure, record terminal replay evidence, and publish a bounded result. An implementation may use existing internal substeps, but no repair may reverse these ownership rules or create a gap in which both parties own value or neither can recover it.

The finite-stock and infinite-stock paths must share request validation, authoritative pricing, payment, commit, delivery, claims, replay, and diagnostics. Their only intended difference is that finite stock creates and settles a durable bounded stock reservation while infinite stock does not decrement a finite quantity. The reproduction must find the earliest actual divergence rather than assuming the reservation itself is faulty.

### Persistence and Recovery Boundary

- Every evidence-bearing original is immutable. Copy the complete matching world, `playerdata`, FutureShops SavedData, journal, checkpoint, ledger, custody, claims, configuration, and relevant logs before a mutation test.
- Keep a manifest of source path class, schema or format, size, hash, acquisition time, redaction, related revision, and restore group. Never publish raw player UUIDs, private NBT, or unrelated mod data.
- If a changed schema is necessary, define current and legacy readers, target schema, migration trigger, idempotency, atomic replacement, last-known-good behavior, backup point, rollback boundary, and malformed-data response before implementation.
- A failed test stops the isolated server, preserves the resulting state and logs, and restores the entire matching fixture group. Do not restore or delete one selected file from a multi-file recovery lineage.
- A player-data repair may modify only proven FutureShops-owned state. Platform-level NBT ordering differences are compared canonically, while field presence, values, item identity, capabilities, and unrelated mod payloads must remain equivalent.

### Networking and Client Boundary

The server validates packet direction, authenticated player identity, route or session nonce, request UUID, payload bounds, listing and option identity, quantity, payment source, current catalog revision, readiness, permissions, and authoritative state. The client may display a cached catalog or error result but may not author price, stock, ownership, completion, migration state, or recovery outcome. Stale snapshots produce a stable retry or refresh result without duplicate mutation.

If a result code, payload, or protocol representation must change, preserve compatibility or version the protocol explicitly and test mismatched peers. A logging-only repair must not expose private NBT, balances unrelated to the request, filesystem paths outside approved operator context, or complete player state.

### Concurrency and Performance

- All authoritative mutation occurs on the logical server or through the existing server-thread and synchronized repository contracts.
- Concurrent finite-stock buyers are serialized or compare-and-set against an exact revision. At most available stock succeeds, and every rejection occurs before charge or receives deterministic compensation.
- Reconnect, retransmission, and restart use persisted request and transaction identity. They do not create new logical purchases.
- No new full catalog scan, filesystem operation, player-file force, broad inventory serialization, or unbounded diagnostic runs per tick, render, or packet hot path.
- Recovery work remains bounded per tick. Diagnostics aggregate repeated identical failures and retain one actionable record rather than spam.

## Failure, Recovery, and Edge Cases

| Scenario | Detection | Required behavior | Recovery or rollback | Regression proof |
|---|---|---|---|---|
| Legacy catalog is valid but migration metadata is missing, failed, or stale | Catalog fingerprint, durable store contents, migration stage, listing identity, quantity, reservation, and receipt comparison | Adopt only if the complete existing durable state passes verification; otherwise preserve state and fail closed with exact reason | Restore the matching fixture only after stopping the server; never clear the durable store to force migration | Legacy and current fixture matrix across first startup, repeated startup, restart, and reload |
| One catalog entry is invalid or references a missing registry item | Parser and validator identify filename, listing, field, and stable validation code | Preserve the last valid snapshot and expose bounded actionable diagnostics. Do not silently hide unrelated valid state | Correct a working copy or use documented quarantine tooling only if already authorized and verified | Mixed valid and invalid catalog tests plus runtime last-known-good check |
| Catalog reload races a purchase or stock reservation | Catalog and stock revision mismatch or server-thread assertion | Serialize publication and reconciliation, reject stale operation safely, retain reservation consistency | Retry with refreshed authoritative snapshot and same logical identity when permitted | Concurrent reload and purchase test with snapshot and conservation comparison |
| Escrow remains recovering or migration fails | Runtime state, migration stage, and exact failure record | Keep mutation unavailable, claims reachable, and status actionable. Never publish false readiness | Preserve journal lineage and follow verified recovery; no manual deletion | Delayed readiness, failed migration, restart, status, and claim-access tests |
| Issue 32 evidence is unavailable | `EXT-003` has no preserved state and deterministic reproduction does not match the report | Do not claim a cause or fix. Complete mapping and safe independent tests only | Keep issue and requirement open with a precise evidence request | Completion packet shows blocked status and absence of speculative implementation |
| Player file is malformed before FutureShops runs | Baseline parser or canonical diff fails before the triggering operation | Attribute preexisting invalidity accurately and avoid writes that obscure it | Preserve original and stop; recover only on a verified complete copy | Preexisting corruption control fixture remains unchanged |
| Player inventory mutation succeeds but save or force fails | Receipt remains unconfirmed, save exception, file or directory force result | Do not commit irreversible completion. Retain custody or claim and bounded recovery context | Replay recovery with same identity after storage is healthy; never delete player data | Failure injection before save, after save, during file force, and directory-force fallback |
| Item NBT normalizes across copy or save | Canonical exact-item proof differs only in ordering versus semantic fields | Use canonical comparison that preserves exact semantics; never hash unstable byte ordering as ownership truth | Retain evidence and repair proof logic, not unrelated item data | Compound-key ordering and modded stack round-trip fixtures |
| Player disconnects during mutation or claim delivery | Lifecycle event and nonterminal transaction or receipt | Finish only a durably committed safe step; otherwise retain custody and retry or claim | Reconnect and recovery use same transaction and claim identities | Disconnect at each transaction boundary, then reconnect and restart |
| Finite stock is zero, stale, or lower than requested | Authoritative stock revision and available quantity | Reject before payment and inventory mutation with stable conflict result | Refresh snapshot; a retry revalidates using the same logical request when applicable | Zero, one, maximum, over-request, stale revision, and concurrent buyer tests |
| Checked money total overflows or funds are insufficient | Checked arithmetic or provider preflight | Reject before reservation commit or release the prepared reservation deterministically; never use floating point | No value mutation; record bounded validation result | Maximum quantity and price, overflow, zero, negative, and insufficient-funds tests |
| Economy provider fails before, during, or after settlement | Provider receipt state and injected exception | Fail closed, distinguish definitely-not-applied from unknown, and retain recoverable custody for uncertain outcomes | Reconcile by stable provider and transaction receipt; compensate exactly once if required | Provider fault matrix with conservation after retry and restart |
| Inventory fills after commit | Delivery preflight or insertion result | Preserve purchased item in durable claim; charge and stock remain consistent with the committed purchase | Player collects the claim later, including while modules are unavailable | Full and partially full inventory, claim creation, restart, reconnect, and collection tests |
| Duplicate or conflicting request is replayed | Request UUID and canonical fingerprint comparison | Return the original terminal result for identical replay; reject conflicting fingerprint before mutation | Inspect replay evidence; do not generate a replacement request automatically | Same request same payload, same request changed payload, delayed replay, and restart replay |
| Server stops between any durable transaction steps | Crash-cut fixture and incomplete journal state | Recovery converges to one terminal state with no duplicated or lost item, money, stock, or claim | Restart from captured crash state and run bounded recovery | Crash cut after validation, reservation, payment, commit, delivery, claim, and receipt |
| Diagnostic generation fails or contains sensitive context | Log capture and privacy assertions | Preserve the primary safe failure result and emit a bounded fallback code without raw data | Correct diagnostic separately; do not alter transaction state | Log-content tests for correlation fields, redaction, aggregation, and no private NBT |
| External reporter rejects the merged issue 25 candidate | `EXT-002` result on preserved affected world | Reopen investigation at the exact tested revision and retain all evidence | Reproduce on a new working copy; do not overwrite earlier fixture or claim closure | Reporter environment and result become a new failing regression input |
| Multiplayer environment is unavailable | `EXT-004` status | Run deterministic internal work but leave real multiplayer and endpoint gates open | Schedule exact environment test later; no mocked substitute | Completion packet identifies every unverified runtime row |

## Verification Matrix

| Requirement or task | Static or unit | Integration | Real workflow or runtime | Negative and recovery | Evidence artifact |
|---|---|---|---|---|---|
| `P002-TASK-001` | Source and test inventory, branch and version assertions | Baseline focused suite and full test/build comparison | None | Dirty-state and evidence-custody failure rehearsal | Baseline manifest with exact revision and hashes |
| `P002-TASK-002` | Fixture schema, canonical hash, and reset tests | Load each fixture into its intended harness | Create and restore disposable server and client states | Corrupt copy, missing member, mismatched hash, redaction check | Fixture manifest and restore log |
| `CORE-REQ-004`, Tasks 3 and 4 | Catalog parser, offer validator, schema, migration SavedData, migrator, cutover, stock repository, readiness, and snapshot tests | Update, startup, restart, reload, buy, direct sell, and bulk-sell routing from one authoritative catalog | Dedicated server plus client on preserved or equivalent world copy; `EXT-002` reporter retest | Invalid entry, missing registry item, stale or failed migration, nonempty durable store, reload failure, concurrent purchase | Before-and-after lifecycle trace, fixture diff, server and client logs, reporter link |
| `P002-TASK-005` | Writer and call-path coverage assertions | Player mutation and recovery ownership matrix review | Non-mutating inspection of fixture copies | Preexisting malformed data and unsupported ownership controls | State-ownership matrix and source references |
| `CORE-REQ-005`, Task 6 | Serialization, canonical NBT, player lifecycle, receipt, durability barrier, exact item proof, claim, and recovery tests | Trigger operation through save, transaction, claim, restart, and reconnect | Dedicated server with affected or deterministic player state and at least one independent client | Save and force faults, malformed NBT, incompatible schema, unrelated modded inventory, repeated recovery | `EXT-003` sanitized field diff, exact fixture hash, lifecycle logs, conservation report |
| `P002-TASK-007` | Quote, stock math, request fingerprint, and service-level paired fixture test | Full finite versus infinite path trace | Isolated two-client dedicated server when `EXT-004` is available | No actionable log is itself a failing diagnostic assertion | Paired causal trace with first divergence |
| `CORE-REQ-006`, Task 8 | Purchase service, stock repository, SavedData codec, commit codec, replay ledger, claim, money, and checked arithmetic tests | Reservation, provider, escrow, journal, delivery, claim, result packet, persistence, and replay | Two clients buy the same finite listing; repeat after disconnect, restart, and reconnect | Insufficient funds, zero or stale stock, concurrent buyers, full inventory, provider fault, duplicate and conflicting replay, crash cuts | `EXT-004` logs and state snapshots plus request-level conservation report |
| `P002-TASK-009` | Complete focused regression set | Cross-defect catalog, stock, player, escrow, claim, and lifecycle matrix | Server and client smoke with repaired combined revision | Every injected boundary restores or converges safely | Integration matrix and evidence invalidation map |
| `P002-TASK-010` | Literal path, command, version, terminology, and link checks | Source-to-document and issue-to-evidence review | Operator follows backup and recovery procedure on disposable copy | Stop conditions and no-deletion guidance verified | Documentation diff and rehearsal record |
| `P002-TASK-011` | `bash ./gradlew test` | Applicable `bash ./gradlew runData`, `bash ./gradlew runGameTestServer`, then `bash ./gradlew build` | `bash ./gradlew runServer`, `bash ./gradlew runClient`, multiplayer, restart, reconnect, reload | Repeat affected fault and recovery matrices after full build | Exact command outputs, JAR inventory, complete diff, revision manifest |
| `P002-TASK-012` | Signed commit and metadata inspection | Required checks and private independent review | Verify merged revision from fresh `origin/1.20.1` | Failed check or review finding returns to Task 9 or 11 and invalidates downstream evidence | Pull request, review, check, merge, ancestry, issue, and blocker links |

### Fixture and Environment Requirements

- The issue 25 matrix needs at least a current valid catalog, a compatible legacy catalog, a world with empty durable stock, a world with complete verified durable stock but missing or stale migration metadata, a conflicting or partial durable store, a mixed valid and invalid catalog, and a reload candidate that fails validation. Preserve listing IDs, exact NBT, finite and infinite stock, reservations, fingerprints, and migration stages.
- The issue 32 matrix needs a clean player state, the exact affected state if supplied, a deterministic field-equivalent state if produced, complete matching FutureShops persistence, unrelated modded inventory sentinels, an operation sequence, and crash copies around every write and durability boundary. Record semantic and byte-level differences separately.
- The issue 34 matrix uses one stable shop and listing identity, one item, one price in integer minor units, one finite quantity and one infinite policy control, at least two buyers, sufficient and insufficient balances, empty and full inventory states, stable request UUIDs, and deterministic provider fault points.
- Dedicated multiplayer evidence must record exact source commit, JAR hash, Java, Minecraft, Forge, complete FutureShops config, relevant mod list, world and fixture hashes, server identity, independent clients, timestamps, request IDs, and time-ordered server and client logs.
- Production worlds and live player files are never test targets. Reporter testing uses a stopped-server backup or isolated copy.

### Rerun Order and Failure Interpretation

For each repair, run the new focused regression first, then the surrounding component suite. At the frozen integration revision, run all focused issue 25, 32, and 34 tests; `bash ./gradlew test`; `bash ./gradlew runData` only when generated resources or providers changed or when required by the repository gate; `bash ./gradlew runGameTestServer` when applicable GameTests cover the behavior; `bash ./gradlew build`; dedicated-server smoke; client smoke; controlled multiplayer; restart, reconnect, and reload; JAR inspection; and complete diff inspection.

A failing regression before repair is expected evidence. A failure after repair, skipped required test, flaky result, unavailable runtime, timeout without a decisive ready state, conservation mismatch, maintenance state, inaccessible claim, or changed fixture hash is not a pass. Record the exact command, revision, environment, decisive error, and unverified rows. A later material source, schema, fixture, configuration, or documentation change reruns every downstream check it can affect.

## Documentation, Operations, and Release

- Update `README.md` only when user-visible compatibility, installation, behavior, diagnostics, or verification commands change.
- Update `DOCUMENTATION.md` with the verified catalog readiness flow, finite-stock transaction boundary, player-state ownership and recovery boundary, persistence or protocol changes, and exact operator diagnostics.
- Update `docs/backup-restore.md` for any changed backup scope, stop condition, recovery procedure, schema compatibility, or issue 32 preservation guidance. Procedures must stop the server, preserve one complete matching snapshot, validate hashes, restore the whole recovery group, and never recommend deleting player or market state.
- Update `docs/compatibility-matrix.md` for any verified schema, loader, mod-data, item-NBT, or rollback compatibility finding.
- Update `docs/community-bug-regression-test-gaps.md` with closed deterministic gaps and remaining external evidence gaps. Do not represent unavailable reporter or multiplayer evidence as passed.
- Update `docs/README.md` links if a focused document is added. Add no document unless the verified change cannot be maintained clearly in the established layout.
- Keep issue 25, issue 32, and issue 34 records synchronized with exact reproduction or blocker state, sanitized evidence, changed revision, verification results, tested artifact identity, reporter request, and closure status. GitHub text follows repository lowercase and punctuation rules.
- A persistence or config representation change requires a compatibility table, backup and restore instructions, repeat-safe migration procedure, rollback boundary, and exact fixture references before merge.
- No public release, CurseForge or Modrinth upload, GitHub Release, announcement, stable designation, or public release tag is authorized. The phase preserves the master candidate identity `3.0.0-beta.2` without independently publishing it.
- The phase pull request is authorized by the standing phase workflow only after local verification passes. It uses the repository-required merge method, required checks, and one private independent review when available. It never bypasses checks or pushes integration directly to `1.20.1`.

## Risks and Evidence Invalidation

| Risk | Prevention | Detection | Recovery | Evidence invalidated | Reverification |
|---|---|---|---|---|---|
| Reporter evidence does not match the inferred fixture | Treat equivalent fixtures as justified models, not the reporter's exact world | Hash, version, config, schema, and state comparison reveals mismatch | Preserve both fixtures and revise the causal classification | Reporter acceptance and exact reproduction rows | Rerun issue-specific trace and all affected runtime checks on the exact state |
| Original world or player evidence is mutated | Read-only originals, verified working copies, complete recovery groups | Hash or metadata drift | Stop, preserve the changed copy, reacquire a clean original if available | All evidence derived from the altered original | Rebuild fixtures and repeat from verified hashes |
| Issue 32 is misattributed to FutureShops | Require exact invalid field and causal operation, use preexisting-corruption controls | Failure exists before FutureShops action or outside owned fields | Record non-causation evidence and leave issue open for correct evidence | Any speculative repair claim | Repeat with matching context; do not widen scope without plan revision |
| A migration repair discards compatible stock or hides invalid entries | Full candidate validation, last-known-good snapshot, atomic publication, preservation fixtures | Catalog or stock diff, fingerprint mismatch, missing listing, or broad unavailability | Stop server and restore complete matching snapshot | Issue 25 migration, readiness, restart, and reload proof | Rerun all issue 25 and downstream transaction tests |
| A finite-stock repair charges before a safe reservation or decrements twice | Stable request identity, durable reservation, checked state transitions, conservation assertions | Ledger, stock, inventory, or replay mismatch | Freeze mutation, retain journal lineage, reconcile or compensate once | All issue 34 transaction and multiplayer evidence | Rerun full fault, replay, concurrency, restart, and conservation matrix |
| Player save and FutureShops records diverge | Receipt protocol, explicit durability result, crash-cut tests | Unconfirmed receipt, missing claim, nonterminal transaction, field diff | Preserve both sides and use same-identity recovery from complete snapshot | Issue 32 recovery and any issue 34 delivery proof | Rerun save-boundary crash cuts, restart, reconnect, and claims |
| Cross-defect repair changes shared escrow behavior | Serialize shared edits and maintain an invalidation graph | Diff touches common escrow, claim, stock, inventory, or lifecycle code | Return to integrated Task 9 review | Every downstream issue test using the changed component | Rerun all affected focused suites and complete Forge ladder |
| Diagnostic repair leaks private state or floods logs | Stable codes, allowlisted context, redaction, aggregation and bounds | Log-content and rate tests | Remove sensitive evidence from retained artifacts where possible and correct logging | Diagnostic, security, issue, and runtime evidence | Repeat negative cases and inspect complete logs and artifacts |
| External prerequisite remains unavailable | Request exact evidence early and separate internal from external gates | Blocker register remains unknown or unavailable | Keep issue and requirement open; transition only under master internal-gate rule | Real workflow acceptance for affected requirement | Execute exact `EXT-002`, `EXT-003`, or `EXT-004` procedure when available |
| Late documentation or config change affects behavior | Freeze revision before full ladder and classify every diff | Revision or content hash changes after evidence capture | Reopen verification packet | Relevant tests, runtime results, JAR, docs, review and checks | Rerun from the earliest affected gate |
| Merge result differs from reviewed head | Require exact PR head and inspect merge ancestry and changed files | Merge commit lacks reviewed commit or contains unexpected changes | Stop transition and reconcile through a new reviewed change | Review, checks, runtime packet, and downstream handoff | Verify the exact merged revision and rerun affected gates |
| Pinned platform or support line drifts | Inspect build metadata and branch ancestry; keep NeoForge excluded | Toolchain, version, dependency, or loader diff | Remove unauthorized drift through reviewed phase change | All build, runtime, compatibility, and artifact evidence | Rerun complete Forge verification at the restored contract |

## Phase Completion Packet

The completion packet is stored outside the protected plan set and must contain:

1. The exact phase branch, base commit, signed implementation commits, pull request, reviewed head, merge commit, verified `origin/1.20.1` head, and proof that no NeoForge or unrelated dependency change entered the phase.
2. The refreshed issue 25, 32, and 34 evidence packets, duplicate-search records, affected versions, sanitized attachment manifests, fixture hashes, exact reproduction or blocker states, and links to every implementation and verification record.
3. The issue 25 before-and-after lifecycle trace, root-cause statement, catalog and stock fixture diffs, migration compatibility table if applicable, startup, restart, reload, buy, direct-sell, bulk-sell, server, client, and `EXT-002` reporter result or blocker.
4. The issue 32 state-ownership matrix, exact invalid-state proof if available, original and working-copy hashes, sanitized field-level diff, causal operation, repair boundary, unrelated-data preservation proof, restart, reconnect, repeated transaction and recovery results, or the precise unresolved `EXT-003` evidence request with no claimed fix.
5. The issue 34 paired finite and infinite traces, first divergent boundary, root-cause statement, request and transaction identity map, stock, balance, inventory, escrow, claim, journal, replay and persistence before-and-after states, full fault matrix, two-client results, and zero-loss, zero-duplication conservation report or precise `EXT-004` blocker.
6. Exact focused test commands and outputs, `bash ./gradlew test`, applicable `runData`, applicable `runGameTestServer`, `build`, server and client smoke, multiplayer, restart, reconnect, reload, JAR inventory, and complete diff inspection. Every skipped, flaky, unavailable, or failed row remains explicit.
7. Documentation changes and operator rehearsal results for backups, migration, diagnostics, recovery, compatibility, and known external evidence gaps.
8. Private independent review availability and result, resolved findings, required GitHub checks, merge result, and issue and Project states. Externally blocked issues remain open.
9. The evidence invalidation graph naming which tests and runtime proofs must rerun if catalog, migration, stock, transaction, player inventory, durability, claim, recovery, networking, config, or schema code changes later.
10. A transition declaration that distinguishes full requirement satisfaction from an approved external-blocked internal integration gate and names the exact first action for `CORE-PHASE-003`.

The packet must not contain credentials, private raw logs, unsanitized player data, sensitive NBT, production worlds, local absolute paths in published material, caches, generated run directories, or release publication artifacts.

## Next Transition

Transition only to `CORE-PHASE-003`. Before creating its phase branch, verify through GitHub that the Phase 002 Forge pull request is fully merged, fetch the remote, and prove that `origin/1.20.1` contains the merge commit. The next branch starts from that updated approved head; it must not stack on the Phase 002 branch.

The exact first Phase 003 action is to read `phases/plan-phase-003.md` through EOF, ingest this phase's merged catalog model, validation, price, stock, persistence, and blocker contracts, then verify the existing admin item grid and catalog-write surfaces before implementing issue 33. Phase 003 may begin under the master's internal integration gate only when every Phase 002 source-controlled repair, deterministic check, review, merge, documentation, and downstream compatibility gate has passed. Any unavailable `EXT-002`, `EXT-003`, or `EXT-004` remains attached to its requirement and issue and is carried to `CORE-PHASE-007`; it is not treated as acceptance, issue closure, or full Phase 002 completion.

Do not start `CORE-PHASE-003` while the Forge pull request is open, checks are pending or failed, review findings are unresolved, `origin/1.20.1` lacks the merge commit, a conservation mismatch exists, player-state evidence was destroyed, or a source-controlled Phase 002 defect remains unresolved.
