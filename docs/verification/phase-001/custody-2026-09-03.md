# Phase 001 custody and claim checkpoint

Date: 2026-09-03

This checkpoint covers the durable custody and claim boundaries and the server shop buy and sell coordinator route. It is not a phase completion record. Player shop multi leg settlement, external provider adapters, and the full restart matrix remain unfinished.

## Implemented behavior

* `EconomyCustodySavedData` persists bounded item identity, owner, quantity, content hash, state, schema version, checksum, and clean marker.
* `EconomyClaimSavedData` persists claimant, exact amount, bounded description, state, schema version, checksum, and clean marker.
* `PlayerShopBarterEscrowSavedData` persists exact serialized barter payment stacks, buyer and shop identity, state transitions, schema version, checksum, and clean marker. The player shop path writes `PREPARED` before removal, verifies the collected stack bytes, records `REMOVED` before storage insertion, records `STORED` after insertion, and records `COMPLETE` only after sale delivery.
* Player shop settlement SavedData uses schema version 2 with a deterministic SHA 256 content checksum and clean shutdown marker. It reserves a stable claim request and amount before provider credit, preserves new sales while the claim is in flight, and subtracts only the confirmed claim amount.
* Invalid checksums, malformed entries, duplicate identities, and unknown newer schemas are read only blockers.
* The coordinator holds an entitlement before a provider leg, releases it on a proven rejection, and retains it for ambiguous recovery. Online buys advance through `DELIVERED` to `CLAIMED`. Sells retain `HELD` until stock settlement and then release.
* `BalanceManager` includes journal, custody, claims, barter escrow, and player shop settlement data in startup integrity and clean shutdown marker ordering. Settlement claims continue to recover through their shared journal request without treating ordinary pending proceeds as an incomplete economy mutation. After journal recovery, held item custody, incomplete economy claims, or non terminal barter escrow transitions the lifecycle to `FROZEN` with an operator recovery diagnostic instead of returning to `READY` while durable delivery state is unresolved. Shutdown flushes every index even when the lifecycle is already unavailable or frozen, while the clean marker remains unwritten unless the ordered drain gate succeeds.
* Startup now reconciles every incomplete journal request through the provider's durable outcome lookup before exposing the provider. A confirmed or definitive rejection can return the lifecycle to `READY`; an unknown outcome remains `FROZEN`.

## Verification

`bash ./gradlew test --no-daemon --rerun-tasks` completed successfully. The suite covers journal checksum behavior, custody and claim round trips, invalid transitions, capability refusal before custody, duplicate coordinator requests, ambiguous outcomes, and receipt lookup recovery.

`PlayerShopSettlementSavedDataPagingTest` also covers stable settlement claim identity, proceeds
recorded during an in flight claim, one time completion, checksum tampering refusal, and unclean
marker preservation, and migration from legacy schema version 1 to the checksummed schema version 2.

`EconomyTransactionCoordinatorTest.transferCompensationCreditsSenderAfterCreditRejection` proves a failed transfer credit is compensated by a durable deposit to the original sender and does not debit the sender twice.

`EconomyTransactionCoordinatorTest` also proves transfers refuse before debit when deposit capability is missing, freeze when compensation cannot be confirmed, and execute explicit compensation requests as credited deposits.

`PlayerShopBarterSafetySourceTest` proves pure barter checks settlement persistence and rolls payment back from the configured barter storage, including when barter storage is separate from stock storage. It also proves failed inventory restoration preflights capacity and never drops a stack that was already partially inserted. `PlayerShopBarterEscrowSourceTest` proves exact stack serialization, checksums, clean markers, and lifecycle gating before readiness.

`PlayerShopBarterEscrowSavedDataTest` exercises the saved data on an ephemeral 1.21.1 server. It round trips exact item stacks across an unclean save, rejects checksum tampering, and keeps mismatched removal contents in a recovery required state.

`BalanceManagerRecoverySourceTest` proves startup checks held custody, incomplete economy claims, and incomplete barter escrow after journal reconciliation and freezes with stable operator recovery diagnostics. This closes the previously observed paths where a confirmed provider leg or an interrupted offline proceeds claim could leave durable delivery state unresolved while the lifecycle became ready.

`PlayerShopEntitlementTest` proves checked purchase quantity arithmetic, full bundle entitlement accounting, and deterministic custody hashing for the complete configured delivery plan.

`ShopTransactionUtilInventoryTest` proves a failed inventory insertion performs no partial mutation, so delivery overflow handling cannot insert part of a stack and then drop the original stack again.

`PlayerShopQuantitySafetySourceTest` proves player shop bundle delivery and buyback quantities use checked multiplication rather than overflowing integer arithmetic.

`ShopBuyEventPriceSourceTest` and `TransactionEventPriceSafetySourceTest` prove that transaction event price overrides are validated before any provider mutation. Buy totals are recomputed with checked addition after listeners modify prices, and player shop and direct sell paths reject non positive or negative overrides before custody or balance effects.

`ShopBuyQuantitySafetySourceTest` proves duplicate cart lines and aggregate buy quantities use checked integer addition and reject overflow before stock reservation or economy mutation.

`MoneyConsumeOutcomeTest` proves physical money value aggregation uses checked multiplication and rejects overflow rather than wrapping into a negative balance leg.

`ShopBlockEntityPriceArithmeticTest` proves player shop money, buyback, and barter totals return a rejection sentinel on overflow. `ShopCatalogPriceArithmeticSourceTest` proves server catalog line costs use checked multiplication for ordinary, bundle promotion, and discounted pricing paths.

`bash ./gradlew build --no-daemon` completed successfully.

The bounded dedicated server smoke used `timeout 60s bash ./gradlew runServer --no-daemon`. NeoForge loaded Minecraft 1.21.1, NeoForge 21.1.248, GeckoLib 4.8.4, and FutureShops 2.3.0. The server reached `FutureShops server starting` with no exception, error, or economy failure in `run/logs/latest.log` before the deliberate timeout exit 124.

A second disposable dedicated server run sent `SIGTERM` to the exact server process after `FutureShops server starting`. The log recorded `FutureShops server stopping`, world and dimension saves, and config unload. Gradle returned 143 because the controlled signal terminated the wrapper, but the FutureShops stop hook and clean save sequence completed.

`JAVA_HOME=/usr/lib/jvm/java-21-openjdk-amd64 bash ./gradlew runGameTestServer --no-daemon` completed successfully after the atomic inventory insertion, event price validation, transfer capability, compensation, and quantity bounds guards. One required GameTest loaded the mod, verified the save compatible money registration, confirmed the default internal provider was ready, and completed a server authoritative balance mutation and readback. The server shut down cleanly after all one required test passed.

A second disposable dedicated server run used the same compiled source after the transfer and quantity guards. The server reached `FutureShops server starting`, then the bounded smoke stopped at the timeout after startup. No exception, error, fatal, or economy failure appeared in the startup log. The captured wrapper output is `/tmp/futureshops-runserver-1acc0ff.log` and the server log is `run/logs/latest.log`.

The resulting unpublished jar is `build/libs/futureshops-2.3.0.jar`, built from source commit `36fb150`.

SHA 256: `8c057fc434c03c136a45e69cf4c089dca2bcb7ed4d406ff3ffb394122a918253`

SHA 512: `03531dae122b9babcdb2361cbda0e9faac8a7be784c2ff549e7d514ecd2c7cbaf963f5addeff64ac98d06abc0175d775e1cc0d0ea66ddc17bf9be05c33fe245b`

The full test suite and two clean builds from the same source commit completed successfully. Both clean builds produced the same SHA 256 and SHA 512 values. Archive timestamps are normalized and entry order is reproducible.

The archive contains 394 entries, including the coordinator, journal, custody, claim, settlement, shop transaction, GameTest, and empty structure resources. Build output, run directories, logs, and external dependency jars remain outside tracked source.

The jar contains 394 entries. Its class path scan found no Pixelmon, Vault, Bukkit, Spigot, or bridge classes, while the expected FutureShops coordinator, custody, and settlement classes are present.

The coordinator now publishes `BalanceChangeEvent.Post` only after its `RESOLVED` journal transition. Internal provider pre events retain the mutation reason for each request kind, and command and API paths preserve typed unavailable balance results.

The balance leaderboard keeps internal ranking reads available through the coordinated provider. External or recovering selections send provider lifecycle and typed ranking availability metadata, label the UI limitation separately from provider readiness, and refuse the text `/baltop` command until an authoritative ranking capability is available.

Internal provider mutation receipts now persist in checksummed `futureshops_internal_economy_receipts` SavedData. Round trip, checksum rejection, and clean marker tests pass, so completed internal legs remain discoverable after a normal restart.

Physical right click deposits and `/deposit` reserve custody before consuming coin authorization and route the credit through the coordinator. A proven rejection restores the mint count and stack quantities. `/withdraw` reserves custody before debit, inserts bills before registering mint authorization, and leaves held custody for recovery instead of issuing an unjournaled balance reversal when delivery fails.

Compensation requests retain their parent request lineage. Mint reconciliation derives a child identity from the original deposit, transfer credit failure compensation credits the original sender instead of withdrawing again, and confirmed compensation events report a negative balance delta when they reverse a provider credit.

Recovery that proves a pending provider effect now emits the same confirmed balance event as a live completion, without emitting an event for a rejected or still ambiguous lookup.

## Security review

The scoped review covered the pure barter payment path and its rollback helper in `PlayerShopBlockService`, including the separate barter storage boundary, settlement persistence result, inventory restoration, and dropped overflow behavior. It also covered startup reconciliation of incomplete journal records through the provider lookup boundary. The recovery loop is bounded by the persisted journal snapshot, does not accept client input, and preserves the frozen state when lookup cannot prove an outcome. No new authorization, packet, path, deserialization, secret, or optional dependency exposure was found in these changes. The source scan regression tests and jar classpath scan pass, and the repository secret pattern scan found no matches.

GitHub reports one existing medium Dependabot alert on the default `1.20.1` branch for `org.apache.logging.log4j:log4j-api`. The 1.21.1 runtime resolves Log4j API `2.22.1` transitively through the pinned NeoForge toolchain. The alert is outside this focused change and remains a release review item.

The complete player shop crash and restart matrix still needs runtime injection at every escrow transition. An interrupted non terminal barter escrow is now durable and keeps the economy in recovery rather than being discarded or retried blindly. Exact external provider runtime proof remains an open phase gate.
