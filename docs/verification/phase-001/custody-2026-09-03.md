# Phase 001 custody and claim checkpoint

Date: 2026-09-03

This checkpoint covers the durable custody and claim boundaries, the server shop buy and sell coordinator route, and the player shop buyback escrow route. It is not a phase completion record. The player shop crash matrix, external provider adapters, and the full restart matrix remain unfinished.

## Implemented behavior

* `EconomyCustodySavedData` persists bounded item identity, owner, quantity, content hash, state, schema version, checksum, and clean marker.
* `EconomyClaimSavedData` persists claimant, exact amount, bounded description, state, schema version, checksum, and clean marker.
* `PlayerShopBarterEscrowSavedData` persists exact serialized barter payment stacks, buyer and shop identity, state transitions, schema version, checksum, and clean marker. The player shop path writes `PREPARED` before removal, verifies the collected stack bytes, records `REMOVED` before storage insertion, records `STORED` after insertion, and records `COMPLETE` only after sale delivery.
* `PlayerShopSaleEscrowSavedData` persists exact serialized sale output stacks, buyer and shop identity, state transitions, schema version, checksum, and clean marker. The player shop buy path writes `PREPARED` before barter removal or economy debit, verifies the extracted stack bytes, records `REMOVED` before buyer delivery, and records `DELIVERED` and `CLAIMED` after delivery. Admin shop buys persist and mark freshly minted output removed before charging, then hold coordinator custody until delivery finalization. Failed pre delivery paths are `REFUNDED`; interrupted or unproven transitions are `RECOVERY_REQUIRED`.
* Player shop buyback persists the seller's exact item stacks in `PlayerShopBarterEscrowSavedData` before removal, verifies the removed bytes, records `STORED` before the owner's debit, and completes only after the owner debit and seller credit are confirmed. Admin shop buyback uses the same escrow and records the admin sink as `STORED` before the seller credit. Unsupported, unavailable, ambiguous, or unprovable legs refuse or retain the escrow in `RECOVERY_REQUIRED`; proven rollback restores the exact stacks when storage and inventory capacity permit it.
* Player shop settlement SavedData uses schema version 2 with a deterministic SHA 256 content checksum and clean shutdown marker. It reserves a stable claim request and amount before provider credit, preserves new sales while the claim is in flight, and subtracts only the confirmed claim amount.
* Settlement claims verify local claim delivery and resolution after provider confirmation. A claim finalization exception, unexpected claim state, or settlement persistence failure freezes the lifecycle and leaves the durable claim and settlement request available for recovery instead of reporting success.
* Invalid checksums, malformed entries, duplicate identities, and unknown newer schemas are read only blockers.
* The coordinator holds an entitlement before a provider leg, releases it on a proven rejection, and retains it for ambiguous recovery. Online buys advance through `DELIVERED` to `CLAIMED`. Sells retain `HELD` until stock settlement and then release.
* Server shop delivery never drops a paid reward after a confirmed debit. An insertion or custody claim failure freezes the lifecycle and leaves the durable entitlement unresolved. Server shop sells retain held custody through provider rejection and stock compensation until exact inventory restoration and custody release are proven.
* Physical money deposits restore coins only after a definitive provider rejection. Ambiguous deposits, failed compensation, and custody finalization remain recovery required. Withdraw builds and inserts all minted bill stacks atomically before registering mint records, so a partial payout cannot be silently forgotten.
* Pure barter captures the exact main inventory and offhand slots before ingredient removal. Partial removal, stock reservation rejection, and reward insertion rejection restore that snapshot and report a server error if restoration cannot be proven. Ingredient definitions reject non positive counts and use checked merging.
* Offline `/shopadmin view` balance reads use the typed coordinator result and refuse with the economy unavailable message during missing, draining, recovering, or frozen provider states instead of throwing from the legacy provider view.
* `BalanceManager` includes journal, custody, claims, barter escrow, and player shop settlement data in startup integrity and clean shutdown marker ordering. Settlement claims continue to recover through their shared journal request without treating ordinary pending proceeds as an incomplete economy mutation. After journal recovery, held item custody, incomplete economy claims, or non terminal barter escrow transitions the lifecycle to `FROZEN` with an operator recovery diagnostic instead of returning to `READY` while durable delivery state is unresolved. Shutdown attempts every index flush even when one throws, logs the affected index, always releases in memory handles, and leaves the clean marker unwritten unless the ordered drain gate succeeds.
* Startup now reconciles every incomplete journal request through the provider's durable outcome lookup before exposing the provider. A confirmed or definitive rejection can return the lifecycle to `READY`; an unknown outcome remains `FROZEN`.

## Verification

`bash ./gradlew test --no-daemon --rerun-tasks` completed successfully. The suite covers journal checksum behavior, custody and claim round trips, invalid transitions, capability refusal before custody, duplicate coordinator requests, ambiguous outcomes, and receipt lookup recovery.

`EconomyTransactionCoordinatorTest` now injects journal lookup, append, and replacement failures plus provider capability lookup failure. Each failure is contained before an unsafe provider call or retry. Confirmed outcome finalization failure returns `RECOVERY_REQUIRED` while retaining the durable `EXTERNAL_CONFIRMED` receipt, and ambiguous outcome persistence failure freezes without guessing a rejection. Custodied replay also verifies that the requested custody terminal state was actually reached before replaying a confirmed result.

The same test now covers provider confirmation followed by custody finalization failure. The provider leg remains recorded as resolved, custody remains held, and the lifecycle freezes for recovery. A replayed confirmed journal record without its durable custody record is also refused rather than reported as a successful custodied operation. Journal loading and append operations reject records beyond the bounded 10,000 entry limit and leave the data read only.

`PlayerShopSettlementSavedDataPagingTest` also covers stable settlement claim identity, proceeds
recorded during an in flight claim, one time completion, checksum tampering refusal, and unclean
marker preservation, and migration from legacy schema version 1 to the checksummed schema version 2.

Settlement claims now derive a deterministic request identity from the owner, shop position,
pending proceeds, and lifetime proceeds when no persisted claim identity exists. This lets recovery
reconstruct the same claim after an interrupted save while preserving any existing persisted UUID.
`PlayerShopSettlementSavedDataPagingTest.settlementClaimIdentityIsRecoverableFromStablePendingState`
proves the derivation is stable.

`EconomyTransactionCoordinatorTest.transferCompensationCreditsSenderAfterCreditRejection` proves a failed transfer credit is compensated by a durable deposit to the original sender and does not debit the sender twice.

`EconomyTransactionCoordinatorTest` also proves transfers refuse before debit when deposit capability is missing, freeze when compensation cannot be confirmed, and execute explicit compensation requests as credited deposits.

Compensation and refund routing now use the deposit capability and provider operation for the coordinator, legacy coordinated provider, internal retry, and player shop helper paths.

`PlayerShopBarterSafetySourceTest` proves pure barter checks settlement persistence and rolls payment back from the configured barter storage, including when barter storage is separate from stock storage. It also proves failed inventory restoration preflights capacity and never drops a stack that was already partially inserted. `PlayerShopBarterEscrowSourceTest` proves exact stack serialization, checksums, clean markers, and lifecycle gating before readiness.

`ShopBarterSafetySourceTest` proves pure barter uses exact inventory snapshots for rollback and checked ingredient validation. `ShopTransactionUtilInventoryTest` proves the snapshot restores the main inventory and offhand without partial insertion.

`PlayerShopSaleEscrowSourceTest` also proves both player shop buyback paths check the seller inventory capacity and insertion result when exact stack verification fails. An unproven restore keeps the escrow in `RECOVERY_REQUIRED` instead of marking the item returned.

`PlayerShopBarterEscrowSavedDataTest` and `PlayerShopSaleEscrowSavedDataTest` exercise the saved data on an ephemeral 1.21.1 server. They round trip exact item stacks across an unclean save, reject checksum tampering, keep mismatched removal contents in a recovery required state, and reject oversized barter identifiers both before persistence and during reconstruction. `PlayerShopSaleEscrowSourceTest` proves the player shop path prepares, verifies, delivers, claims, refunds, and freezes exact sale stacks, while `BalanceManagerRecoverySourceTest` proves an incomplete sale escrow freezes startup.

`BalanceManagerRecoverySourceTest` proves startup checks held custody, incomplete economy claims, and incomplete barter escrow after journal reconciliation and freezes with stable operator recovery diagnostics. This closes the previously observed paths where a confirmed provider leg or an interrupted offline proceeds claim could leave durable delivery state unresolved while the lifecycle became ready.

`PlayerShopEntitlementTest` proves checked purchase quantity arithmetic, full bundle entitlement accounting, and deterministic custody hashing for the complete configured delivery plan.

`ShopTransactionUtilInventoryTest` proves a failed inventory insertion performs no partial mutation, so delivery overflow handling cannot insert part of a stack and then drop the original stack again.

`PlayerShopQuantitySafetySourceTest` proves player shop bundle delivery and buyback quantities use checked multiplication rather than overflowing integer arithmetic.

`ShopBuyEventPriceSourceTest` and `TransactionEventPriceSafetySourceTest` prove that transaction event price overrides are validated before any provider mutation. Buy totals are recomputed with checked addition after listeners modify prices, and player shop and direct sell paths reject non positive or negative overrides before custody or balance effects.

`ShopBuyQuantitySafetySourceTest` proves duplicate cart lines and aggregate buy quantities use checked integer addition and reject overflow before stock reservation or economy mutation.

`MoneyConsumeOutcomeTest` proves physical money value aggregation uses checked multiplication and rejects overflow rather than wrapping into a negative balance leg.

`ShopBlockEntityPriceArithmeticTest` proves player shop money, buyback, and barter totals return a rejection sentinel on overflow. `ShopCatalogPriceArithmeticSourceTest` proves server catalog line costs use checked multiplication for ordinary, bundle promotion, and discounted pricing paths.

`bash ./gradlew build --no-daemon` completed successfully.

The bounded dedicated server smoke used `timeout 60s bash ./gradlew runServer --no-daemon`. NeoForge loaded Minecraft 1.21.1, NeoForge 21.1.248, GeckoLib 4.8.4, and FutureShops 2.3.0. The server reached `FutureShops server starting` with no exception, error, or economy failure in `run/logs/latest.log` before the deliberate timeout exit 124.

A second disposable dedicated server run sent `SIGTERM` to the exact server process after `FutureShops server starting`. The log recorded `FutureShops server stopping`, world and dimension saves, and config unload. Gradle returned 143 because the controlled signal terminated the wrapper, but the FutureShops stop hook and clean save sequence completed.

`JAVA_HOME=/usr/lib/jvm/java-21-openjdk-amd64 bash ./gradlew runGameTestServer --no-daemon` completed successfully after the atomic inventory insertion, event price validation, transfer capability, compensation routing, and quantity bounds guards. One required GameTest loaded the mod, verified the save compatible money registration, confirmed the default internal provider was ready, and completed a server authoritative balance mutation and readback. The server shut down cleanly after all one required test passed.

A repeat of `runGameTestServer` against the shutdown flush source at commit `0a7e09f` also completed successfully. NeoForge 21.1.248 loaded FutureShops 2.3.0, one required test passed, and the GameTest server stopped cleanly. The run log is `run/logs/latest.log`.

The bounded client startup smoke used `timeout 120s xvfb-run -a -s '-screen 0 1280x720x24' env JAVA_HOME=/usr/lib/jvm/java-21-openjdk-amd64 bash ./gradlew runClient --no-daemon`. The virtual display allowed GLFW initialization, NeoForge loaded the same 1.21.1 and 21.1.248 runtime, and the log recorded `FutureShops common setup complete` without an exception, fatal error, or FutureShops error before the deliberate timeout exit 124. The captured wrapper output is `/tmp/futureshops-runclient-phase001-xvfb.log`. This is startup and classloading evidence only, not the separate interactive screen fixture.

A final bounded client startup smoke used the same command against source commit `5f88603`. The virtual display initialized GLFW, the log recorded `FutureShops common setup complete`, and no exception, fatal error, or FutureShops error was present in `/tmp/futureshops-runclient-journal-bound-5f88603.log` before the deliberate timeout exit 124.

A second disposable dedicated server run used the same compiled source after the transfer and quantity guards. The server reached `FutureShops server starting`, then the bounded smoke stopped at the timeout after startup. No exception, error, fatal, or economy failure appeared in the startup log. The captured wrapper output is `/tmp/futureshops-runserver-d708fc3.log` and the server log is `run/logs/latest.log`.

The latest `5f88603` build passed the focused tests, clean build, and dedicated server smoke. The smoke loaded Minecraft 1.21.1, NeoForge 21.1.248, GeckoLib 4.8.4, and FutureShops 2.3.0, reached `FutureShops server starting`, and stopped at the bounded timeout without an error, exception, fatal, or economy failure. The log recorded `FutureShops server stopping` during shutdown.

A final bounded dedicated server smoke used `timeout 60s env JAVA_HOME=/usr/lib/jvm/java-21-openjdk-amd64 bash ./gradlew runServer --no-daemon` against source commit `5f88603`. The server reached `FutureShops server starting`, the controlled timeout stopped the wrapper, and `run/logs/latest.log` recorded `FutureShops server stopping` without an exception, error, fatal, or economy failure.

A bounded dedicated server smoke against source commit `10f1650` used the same command. The server reached `FutureShops server starting` and the controlled timeout exited with 124. The captured wrapper output is `/tmp/futureshops-runserver-sell-compensation-10f1650.log`; no exception, error, fatal, or economy failure was present in the startup output or `run/logs/latest.log`.

A bounded dedicated server smoke against source commit `0a7e09f` used the same command. The server reached `FutureShops server starting` and the controlled timeout exited with 124. The captured wrapper output is `/tmp/futureshops-runserver-shutdown-flush-0a7e09f.log`; no exception, error, fatal, or economy failure was present in the startup output or `run/logs/latest.log`.

The resulting unpublished jar is `build/libs/futureshops-2.3.0.jar`, built from source commit `0a7e09f`.

SHA 256: `6c5ebb240db4898de9ff3ba4d3878057f6699062c464443a048aa1a1d3829253`

SHA 512: `edb4c26c1180d3a49873bf48b8e5fc9bc4d86ffe349c6d3e6846a102eb25556003574471991cacd92a4952903f6e434843274593c0c95ed64fc637cb697433b5`

The full test suite and one build from source commit `0a7e09f` completed successfully. Archive timestamps are normalized and entry order is reproducible. The exact commands were `JAVA_HOME=/usr/lib/jvm/java-21-openjdk-amd64 bash ./gradlew test --no-daemon` and `JAVA_HOME=/usr/lib/jvm/java-21-openjdk-amd64 bash ./gradlew build --no-daemon`.

The archive contains 397 entries, including the coordinator, journal, custody, claim, player shop barter escrow, player shop sale escrow, settlement, shop transaction, GameTest, and empty structure resources. Build output, run directories, logs, and external dependency jars remain outside tracked source.

The committed source was rebuilt with `JAVA_HOME=/usr/lib/jvm/java-21-openjdk-amd64 bash ./gradlew build --no-daemon`. The build produced the SHA 256 and SHA 512 values above. The focused custody, transaction, and money safety tests and the complete unit suite passed after this source revision. The barter escrow bounds, sell compensation direction, and shutdown flush safety regressions are included in the focused suite.

The jar contains 397 entries. Its class path scan found no Pixelmon, Vault, Bukkit, Spigot, or bridge classes, while the expected FutureShops coordinator, custody, sale escrow, and settlement classes are present.

The latest source commit `472c6f5` replaces the unchecked balance delta magnitude in `BalanceManager.setInternalBalance` with checked negation. This preserves the valid maximum to zero withdrawal boundary and rejects only an unrepresentable delta. `BalanceManagerRecoverySourceTest.setBalanceUsesCheckedDeltaMagnitude` covers the guard.

The rebuilt unpublished jar from `472c6f5` passes `unzip -tq` and contains 397 entries.

SHA 256: `1e794f679c223774ce002f75f7eee651cd3e060feb1a91daeace2868c07cb979`

SHA 512: `b29053d97e6248aa384d8576b732f33a7b4277e83d051e620198bf15d35f7665e7a69f25fc38b5dd21c08f73144a5de8265ba6b5f2b7dddf526e5ea974123525`

The latest source commit `dd00425` adds deterministic player shop settlement claim identities
for recovery. The full `test` task and `build` task completed successfully, and the rebuilt jar
passes `unzip -tq` with 397 entries.

SHA 256: `a5842ec0ef32cce3a71e686604296d4fc49a24270663863b2d7db8275a012352`

SHA 512: `98ab68eb16e725ebc4be8f1b4ea2779246eb8cbdc2ef7f7b0dcb1b23932f710c003f2e8d18d9957fec222d477723ab7a38467a2b1969115ef81bed7a5693db7c`

A bounded dedicated server smoke against source commit `dd00425` used the same command. The
server reached `FutureShops server starting`; the controlled timeout stopped the wrapper after
startup, and `run/logs/latest.log` recorded `FutureShops server stopping` without an exception,
error, fatal, or economy failure.

The required GameTest run against source commit `dd00425` completed successfully. NeoForge
loaded FutureShops 2.3.0, one required test passed, and the GameTest server recorded a clean
FutureShops stop. Captured output is `/tmp/futureshops-gametest-settlement-dd00425.log`.

The GameTest suite now includes the player shop sale escrow lifecycle. Against source commit
`4dfed11`, both required GameTests passed after a real 1.21.1 server save and restore round trip
through `PREPARED`, `REMOVED`, `DELIVERED`, and `CLAIMED`. Captured output is
`/tmp/futureshops-gametest-sale-escrow-4b70a17.log`.

The rebuilt unpublished jar from `4dfed11` passes `unzip -tq` and contains 399 entries.

SHA 256: `8426e4419d4d0900b002454979fa7ec85e982457e08154910fa85b05d9f93dee`

SHA 512: `1d83d15237c575d71f05344e427bbe93c7241fed7669d8ff08aef124a478aedc7811684b060928d03b71b823ca09a0ca4fcf608c9c949a7acbde1cb1dfcaf2f2`

A bounded dedicated server smoke against source commit `4dfed11` reached `FutureShops server
starting`; the controlled timeout stopped the wrapper after startup, and `run/logs/latest.log`
recorded `FutureShops server stopping` without an exception, error, fatal, or economy failure.

The bounded dedicated server smoke used `timeout 60s env JAVA_HOME=/usr/lib/jvm/java-21-openjdk-amd64 bash ./gradlew runServer --no-daemon`. NeoForge loaded Minecraft 1.21.1, NeoForge 21.1.248, GeckoLib 4.8.4, and FutureShops 2.3.0, and reached `FutureShops server starting` with no exception, error, fatal, or economy failure before the deliberate timeout exit 124. Captured output is `/tmp/futureshops-runserver-sale-escrow-4677647.log`.

The coordinator now publishes `BalanceChangeEvent.Post` only after its `RESOLVED` journal transition. Internal provider pre events retain the mutation reason for each request kind, and command and API paths preserve typed unavailable balance results. The server shop sell rollback now uses a stable `WITHDRAW` reversal leg after a post credit stock failure, matching the provider operation and avoiding a mismatched `COMPENSATION` request rejection.

The coordinator failure matrix now covers custody release failure after a definitive provider rejection. The provider result remains rejected, the custody record remains held for operator recovery, the journal remains resolved with the rejection, and lifecycle admission freezes instead of guessing that the item was restored. Settlement history decoding now rejects oversized persisted type and item identifiers before reconstructing owner rows. The focused tests, complete `test` task, and `build` task pass after these guards.

The balance leaderboard keeps internal ranking reads available through the coordinated provider. External or recovering selections send provider lifecycle and typed ranking availability metadata, label the UI limitation separately from provider readiness, and refuse the text `/baltop` command until an authoritative ranking capability is available.

Internal provider mutation receipts now persist in checksummed `futureshops_internal_economy_receipts` SavedData. Round trip, checksum rejection, and clean marker tests pass, so completed internal legs remain discoverable after a normal restart.

Physical right click deposits and `/deposit` reserve custody before consuming coin authorization and route the credit through the coordinator. A proven rejection restores the mint count and stack quantities. `/withdraw` reserves custody before debit, inserts bills before registering mint authorization, and leaves held custody for recovery instead of issuing an unjournaled balance reversal when delivery fails.

Compensation requests retain their parent request lineage. Mint reconciliation derives a child identity from the original deposit, transfer credit failure compensation credits the original sender instead of withdrawing again, and confirmed compensation events report a negative balance delta when they reverse a provider credit.

Recovery that proves a pending provider effect now emits the same confirmed balance event as a live completion, without emitting an event for a rejected or still ambiguous lookup.

## Security review

The scoped review covered the pure barter payment path and its rollback helper in `PlayerShopBlockService`, including the separate barter storage boundary, settlement persistence result, inventory restoration, and no drop overflow behavior. It also covered startup reconciliation of incomplete journal records through the provider lookup boundary. The recovery loop is bounded by the persisted journal snapshot, does not accept client input, and preserves the frozen state when lookup cannot prove an outcome. No new authorization, packet, path, deserialization, secret, or optional dependency exposure was found in these changes. The source scan regression tests and jar classpath scan pass, and the repository secret pattern scan found no matches.

GitHub reports one existing medium Dependabot alert on the default `1.20.1` branch for `org.apache.logging.log4j:log4j-api`. The 1.21.1 runtime resolves Log4j API `2.22.1` transitively through the pinned NeoForge toolchain. The alert is outside this focused change and remains a release review item.

The complete player shop crash and restart matrix still needs runtime injection at every escrow transition. An interrupted non terminal barter escrow is now durable and keeps the economy in recovery rather than being discarded or retried blindly. Exact external provider runtime proof remains an open phase gate.

The latest GameTest run completed with both required tests passing after the settlement text bounds and custody release failure coverage. Captured output is `/tmp/futureshops-gametest-settlement-bounds.log`. A bounded dedicated server smoke reached `FutureShops server starting` without an exception, error, fatal, or economy failure before the deliberate timeout. Captured output is `/tmp/futureshops-runserver-settlement-bounds.log`.

The rebuilt unpublished jar after these source guards passes `unzip -tq` and contains 397 entries.

SHA 256: `2702b6ab2b7f062b2d7706402d23dddcb6f0b9a29a8804924db29f2e39abbdfe`

SHA 512: `e01f36e8861fac0472dc577036cecbb9b415cf001dc06cf3527231bdcaefc0caae74b58f29294bf209d95d46910e1f308c8d924a7619728ae7221c76231b292b`

The latest source commit `82f2a17` prevents partial journal recovery. Journal loading now stages every decoded record and publishes none when any record is malformed, duplicated, or checksum invalid. Startup recovery also freezes when any required persisted economy index reports invalid integrity instead of marking the lifecycle ready after a partial read. The focused persistence tests, full unit suite, build, two required GameTests, and dedicated server smoke passed.

The rebuilt unpublished jar from `82f2a17` passes `unzip -tq` and contains 397 entries.

SHA 256: `cabb41d3362bf618b8e07df09d966ce14b4199f1a05260a68c41b6c6f56c38b7`

SHA 512: `95dd8e57975697aa988c7db71369b3a339a947ab8186d4a00da67f019f82cff8f2318186ff1c108ceb0dc631e3b5bd747e44ed1629e89fad6188c6298dd0d01f`

The GameTest capture is `/tmp/futureshops-gametest-journal-integrity-fa3ebe2.log`, with both required tests passing and a clean FutureShops stop. The bounded server capture is `/tmp/futureshops-runserver-journal-integrity-fa3ebe2.log`; it reached `FutureShops server starting` with no exception, error, fatal, or economy failure before the deliberate timeout.

The latest source commit `32d150a` binds replay to the persisted request payload and custody metadata. Reusing a request id with a changed actor, counterparty, amount, kind, item key, quantity, or content hash is rejected without a provider call or lifecycle freeze. The focused coordinator suite, complete `test`, `build`, two required GameTests, and dedicated server smoke passed.

The rebuilt unpublished jar from `32d150a` passes `unzip -tq` and contains 397 entries.

SHA 256: `41239049430533e85501d4caa89346a70fffd3069327cdf704aeccfc66debc48`

SHA 512: `9e22810ec1e0723cd94144c93173f307069b2e744c65ca0a230c859b95d46e4bbc95c71ead3305fc47bfa20dccbb45227e4261566427917df85f3b44dcd17101`

The GameTest capture is `/tmp/futureshops-gametest-request-binding-82f2a17.log`, with both required tests passing and a clean FutureShops stop. The bounded server capture is `/tmp/futureshops-runserver-request-binding-82f2a17.log`; it reached `FutureShops server starting` with no exception, error, fatal, or economy failure before the deliberate timeout.

The latest source commit `a0bfe46` binds durable journal records to their originating provider. New records persist the provider id in the checksummed entry, replay and recovery refuse records bound to another provider, and legacy entries without a binding are treated as unbound recovery evidence instead of silently switching providers. Receipt semantics are validated before a journal entry is loaded as confirmed. The focused journal and coordinator suites, complete `test`, `build`, two required GameTests, and dedicated server smoke passed.

The rebuilt unpublished jar from `a0bfe46` passes `unzip -tq` and contains 397 entries.

SHA 256: `b6703dc691d541292132d6e4fc0a76cdb6e0ecc242ce74e6d8db441ad24113b3`

SHA 512: `d435d27f338350120a3c541298395811aeef5e1f9d8155c8a5ee606905f7fb1801d03b6da8f8ac6b9605de421ce047bfb65f6da1b6c724bd9fb75f5ffc557fdc`

The GameTest capture is `/tmp/futureshops-gametest-provider-binding-0f3da81.log`, with both required tests passing and a clean FutureShops stop. The bounded server capture is `/tmp/futureshops-runserver-provider-binding-0f3da81.log`; it reached `FutureShops server starting` with no exception, error, fatal, or economy failure before the deliberate timeout.

The latest source commit `28e2879` hardens public physical coin totals and the administrator coin audit against multiplication and aggregation overflow. Overflow now produces a localized failure instead of wrapping a monetary total. The focused source checks, complete `test`, `build`, two required GameTests, and dedicated server startup smoke passed.

The rebuilt unpublished jar from `28e2879` passes `unzip -tq` and contains 397 entries.

SHA 256: `c81a5d022de7d2b140104b48c1d2429151be96c9b052ea58f882e6f13216a00a`

SHA 512: `1669b1d73c9b8210102450c0137afe53f214e970f0ffb06c79c05820338416c8e1e3f7fd94c4085e9d4e90cc1ac2c65153c3584a9f79295d0d07037b9e8e2ff9`

The GameTest capture is `/tmp/futureshops-gametest-coin-overflow.log`, with both required tests passing and a clean FutureShops stop. The bounded server capture is `/tmp/futureshops-runserver-coin-overflow.log`; it reached `FutureShops server starting` with no exception, error, fatal, or economy failure before the deliberate timeout.

The latest source commit `d358b69` hardens the physical mint ledger. Loaded mint records are staged and bounded to 10,000 entries, malformed or duplicate ids are discarded, text fields have length limits, and count, denomination, timestamp, and consumed state invariants are enforced. Registration rejects invalid or over-capacity records. Registry reads, writes, registration, consumption, and restoration are synchronized so concurrent deposits cannot redeem the same mint batch twice. The focused mint ledger tests cover malformed decoding, duplicate rejection, save round trip, anti-duplication, restoration, and concurrent consumption. The complete `test` task, `build` task, required GameTests, and dedicated server startup smoke passed.

The rebuilt unpublished jar from `d358b69` passes `unzip -tq` and contains 397 entries.

SHA 256: `c0cbc3e6b4ff7c62c49053f8087f79b3a89d3449e6475e01cc67784b08a557ff`

SHA 512: `6f14112ac7ffc8e8b442edcc00ef14e6fae06c2ae78b1bab0c7293717e9ca5d7cb635d5caa5c747fadae0d0efd66855cb3972d60cf3c42ef8a81c6f050fbd602`

The GameTest capture is `/tmp/futureshops-gametest-mint-ledger.log`, with both required tests passing and a clean FutureShops stop. The bounded server capture is `/tmp/futureshops-runserver-mint-ledger.log`; it reached `FutureShops server starting` with no exception, error, fatal, or economy failure before the deliberate timeout exit 124.

The latest source commit `82a4050` changes malformed admin catalog SNBT handling to fail closed. A parse or migration failure now rejects the buy before reward construction, inventory mutation, stock reservation, custody, or provider invocation. The prior bare item fallback is removed because it could deliver a different variant than the catalog requested. The focused source regression, complete `test` task, `build` task, required GameTests, and dedicated server startup smoke passed.

The rebuilt unpublished jar from `82a4050` passes `unzip -tq` and contains 397 entries.

SHA 256: `708cb1d6ec8cf1590711dc341bacf27008764704b7469e835a52b3e4737f7ba9`

SHA 512: `8b3b45dfaa8c9c2fb75863097a0f5952e25e06137780ff06fb864b9cc5eb31beaa07ae1ccb50d61dbd9bd8b50e0eab15382635d4e58ef1b4f0a5f2547a4795d7`

The GameTest capture is `/tmp/futureshops-gametest-snbt-rejection.log`, with both required tests passing and a clean FutureShops stop. The bounded server capture is `/tmp/futureshops-runserver-snbt-rejection.log`; it reached `FutureShops server starting` with no exception, error, fatal, or economy failure before the deliberate timeout exit 124.

The latest source commit `302f7dc` also rejects malformed cart lines as a whole. Null, blank, overlong, nonpositive, oversized, or excessive line inputs now produce no merged cart, so a valid line cannot execute while a malicious line is silently dropped. The focused cart safety test, complete `test` task, `build` task, required GameTests, and dedicated server startup smoke passed.

The rebuilt unpublished jar from `302f7dc` passes `unzip -tq` and contains 397 entries.

SHA 256: `eacb682e7f6a4e6b8739c957f6c3482983b1ff8c1682b754f303099428293953`

SHA 512: `96109b5bb198ec40f7b1e779fdf4e7510c2f3986a926003df0bea968a77ec888c85591467b2352f45d3ec61c699964c5e2183fecb9995f2f9246b7f6bd5496ee`

The GameTest capture is `/tmp/futureshops-gametest-cart-bounds.log`, with both required tests passing and a clean FutureShops stop. The bounded server capture is `/tmp/futureshops-runserver-cart-bounds.log`; it reached `FutureShops server starting` with no exception, error, fatal, or economy failure before the deliberate timeout exit 124.

The latest source commit `ddc6be5` now rejects malformed sell listing SNBT before inventory removal or any provider effect. A parse or migration failure returns the existing invalid item result and leaves the seller inventory and economy unchanged. The focused sell input safety test, complete `test` task, `build` task, required GameTests, and dedicated server startup smoke passed.

The rebuilt unpublished jar from `ddc6be5` passes `unzip -tq` and contains 397 entries.

SHA 256: `eb8fa9e859d0c4ac7172130c3b21a060ed40896082b5cdbc58853b8225c0c041`

SHA 512: `575115d00af6e1adae453197119ac955362682340a3a4b1bbc862a7dc97aa05b86fe44ce6144368f9139a2ad03e3bee59a037576512dafe3370c05ac7ba8909c`

The GameTest capture is `/tmp/futureshops-gametest-sell-input.log`, with both required tests passing and a clean FutureShops stop. The bounded server capture is `/tmp/futureshops-runserver-sell-input.log`; it reached `FutureShops server starting` with no exception, error, fatal, or economy failure before the deliberate timeout exit 124.

The latest source commit `a16657c` makes player shop item escrow recovery atomic at load. Barter and sale escrow now stage every record and reject the complete dataset when any record, record list, stack list, checksum, duplicate identity, or persisted identifier is malformed. Stack transition serialization also catches provider or item encoding failures before changing escrow state. This prevents a valid record from being used after a partial or ambiguous recovery read. The focused escrow suites, complete `test` task, `build` task, required GameTests, and dedicated server startup smoke passed.

The rebuilt unpublished jar from `a16657c` passes `unzip -tq` and contains 397 entries.

SHA 256: `3d75d0f4355672b778abf01f5a9d65c515fec038dfadd73fe6ad7c988330bb59`

SHA 512: `52615e56b805eb0fcc3067117b0c84edc12814f368c4bdccdb27dcb6dbf30f508f42f55d39cd4051ef51a814fc3baae2d5ae36b89f0887a5104ece892a80f69d`

The GameTest capture is `/tmp/futureshops-gametest-escrow-integrity-a16657c.log`, with both required tests passing and a clean FutureShops stop. The bounded server capture is `/tmp/futureshops-runserver-escrow-integrity-a16657c.log`; it reached `FutureShops server starting` with no exception, error, fatal, or economy failure before the deliberate timeout exit 124.

The latest source commit `67e5c53` rejects zero, negative, and over-limit player shop buyback quantities instead of coercing invalid packets to one item. The focused quantity safety test, complete `test` task, `build` task, required GameTests, and dedicated server startup smoke passed.

The rebuilt unpublished jar from `67e5c53` passes `unzip -tq` and contains 397 entries.

SHA 256: `4e7bacb6fefaf34a2d13e7f84fdb3144a7e1a8301f5f8831edce5db8b5407cea`

SHA 512: `8a7f776a2ccef9946ab78d295f4ec81839aa6f2b3ee4055cb29c021d0984ff322c2ac5dcaf9b07d92cd95985d90843e87bf616661627e9609dee49d01c9348a9`

The GameTest capture is `/tmp/futureshops-gametest-buyback-quantity-2026-09-03.log`, with both required tests passing and a clean FutureShops stop. The bounded server capture is `/tmp/futureshops-runserver-buyback-quantity-2026-09-03.log`; it reached `FutureShops server starting` with no exception, error, fatal, or economy failure before the deliberate timeout exit 124.

The latest source commit `64caf9e` hardens transaction history persistence and presentation inputs. History records now use bounded text and quantity fields, reject malformed or newer schemas, stage complete loads without partial reconstruction, enforce player and row limits, and serialize access consistently. Null filters are normalized and oversized searches are rejected. Invalid server shop sell quantities now return `INVALID_AMOUNT` instead of `INVALID_ITEM`. The focused history and sell input suites, complete `test` task, `build` task, required GameTests, and dedicated server startup smoke passed.

The rebuilt unpublished jar from `64caf9e` passes `unzip -tq` and contains 397 entries.

SHA 256: `96f168c2799c3392365cfcd847734e3dcac52de0b560dab2b9644044935ca8bc`

SHA 512: `36c6115ee2e401f50a4be85d76734ea4ab5875a7758057d6fd0504cba96f0198c291ff10d7c03d4ab3bccb2ae729a45f2849a28ecb54c6309872d349aebaf80d`

The GameTest capture is `/tmp/futureshops-gametest-history-safety-2026-09-03.log`, with both required tests passing and a clean FutureShops stop. The bounded server capture is `/tmp/futureshops-runserver-history-safety-2026-09-03.log`; it reached `FutureShops server starting` with no exception, error, fatal, or economy failure before the deliberate timeout exit 124.

The latest source commit `25aa64a` hardens internal balance persistence. Malformed, duplicate, oversized, wrong-type, or newer balance data now enters an invalid integrity state without partial reconstruction or a server-crashing cast. Internal startup includes this integrity result in the economy readiness gate, so corrupted internal balances cannot be treated as ready. Balance reads, writes, and snapshots are synchronized. The focused internal balance suite, complete `test` task, `build` task, required GameTests, and dedicated server startup smoke passed.

The rebuilt unpublished jar from `25aa64a` passes `unzip -tq` and contains 397 entries.

SHA 256: `d300b9128e738a28a0a5bd3d2695092f619e0125654f585f928275d11d97c241`

SHA 512: `5cb3ea1f317dc7d6d1f844ea5c365347fb266154c5d481fb4000a242c4045dfc4250753e06bcd260809686a4972313fa6da94c02086f3161f560953b2d10e0a2`

The GameTest capture is `/tmp/futureshops-gametest-internal-balance-integrity-2026-09-03.log`, with both required tests passing and a clean FutureShops stop. The bounded server capture is `/tmp/futureshops-runserver-internal-balance-integrity-2026-09-03.log`; it reached `FutureShops server starting` with no exception, error, fatal, or economy failure before the deliberate timeout exit 124.

The latest source commit `a7605db` hardens player shop registry recovery. Owner and shop records are staged and bounded, wrong tag types, missing fields, duplicate owners, malformed dimensions, and newer schemas invalidate the complete registry instead of crashing or exposing a partial index. Registration now rejects null identifiers and per-owner overflow. The focused registry suite, complete `test` task, `build` task, required GameTests, and dedicated server startup smoke passed.

The rebuilt unpublished jar from `a7605db` passes `unzip -tq` and contains 397 entries.

SHA 256: `17d9cb1042a8c782c5ec8d19b5dae70f5c69426f41355b7dde777d526dc9dd64`

SHA 512: `e2eaa11e69b1349d6caf7e40c3291aa9dbeb0d7ee3ccf1a61f1c53f73090a822cbf413671b251a7fd8bba70d98ea18cc253311a1db7278fb2107dc876b88e03e`

The GameTest capture is `/tmp/futureshops-gametest-registry-a7605db.log`, with both required tests passing and a clean FutureShops stop. The bounded server capture is `/tmp/futureshops-runserver-registry-a7605db.log`; it reached `FutureShops server starting` with no exception, error, fatal, or economy failure before the deliberate timeout exit 124.

The latest source commits `fc28a48`, `0e768bb`, `58ec2f7`, and `b1dad4d` extend the recovery proof. Provider mutation exceptions and mismatched receipts now have explicit coordinator regression coverage. Journal, custody, claim, and settlement decoders reject wrong record container types, while registry recovery also rejects duplicate references and invalid dimension identifiers. The complete `test` task, `build` task, required GameTests, and dedicated server startup smoke passed after the combined changes.

The rebuilt unpublished jar from `b1dad4d` passes `unzip -tq` and contains 397 entries.

SHA 256: `e5432a2a5187dbd2582f6606e815052cd11bef878f24611ec70f1146265d1e52`

SHA 512: `b3d7b43b07db3d72dcf0953e1a283281f61a0b9307f5284c91a38aa8e61a10ef06e4d4dab4d133cd5f78cea58a143acfdbaf3d553b88d33795abea193839df5d`

The GameTest capture is `/tmp/futureshops-gametest-persistence-b1dad4d.log`, with both required tests passing and a clean FutureShops stop. The bounded server capture is `/tmp/futureshops-runserver-persistence-b1dad4d.log`; it reached `FutureShops server starting` with no exception, error, fatal, or economy failure before the deliberate timeout exit 124.

The latest source commit `16a20a8` applies the same wrong-container rejection to internal provider receipt persistence. The complete `test` task, `build` task, required GameTests, and dedicated server startup smoke passed after the change.

The rebuilt unpublished jar from `16a20a8` passes `unzip -tq` and contains 397 entries.

SHA 256: `6a0765536df5c5293cd9cd62c67406e28c24445def26c85fa890f197c2c91b6d`

SHA 512: `6f2a7c40050edee716f4801ab45988ae0fd0e8ef58d85da994efc7a36d283bb4a9e8528d447a95be9370f16e9f450ebdec63e744cad01c81e3fa90c3240a4aa8`

The GameTest capture is `/tmp/futureshops-gametest-receipt-persistence-16a20a8.log`, with both required tests passing and a clean FutureShops stop. The bounded server capture is `/tmp/futureshops-runserver-receipt-persistence-16a20a8.log`; it reached `FutureShops server starting` with no exception, error, fatal, or economy failure before the deliberate timeout exit 124.

The current source hardens franchise ownership persistence. Franchise loads now reject newer schemas, wrong container types, missing fields, invalid names or timestamps, duplicate franchise identifiers, duplicate player membership, missing leaders, and malformed member records without partially reconstructing the ownership index. Public franchise operations also reject null identifiers and enforce the existing member and name bounds. `FranchiseSavedDataTest` covers valid round trips, wrong containers, malformed later records, duplicate membership, and newer schemas.

The current source also removes floating point arithmetic from dynamic price updates. Configured weights, decay, bounds, demand, supply, clamping, and minor unit rounding now use checked `BigDecimal` operations. Invalid counts, non finite configuration values, and values outside the signed minor unit range are rejected without resetting activity counters. `DynamicPricingEngineTest` covers exact half up minor unit rounding, invalid inputs, and overflow refusal.

Dynamic pricing persistence now rejects newer schemas, wrong state containers, oversized or malformed keys, missing typed fields, invalid counters, and invalid prices without partially reconstructing the price state. Activity recording enforces bounded positive quantities and a bounded state count. `DynamicPricingSavedDataTest` covers round trips, wrong containers, malformed state entries, and invalid activity.

The source commit is `38bed89`. `bash ./gradlew test --no-daemon` and `bash ./gradlew build --no-daemon` passed. The required GameTest run passed both tests and stopped cleanly in `/tmp/futureshops-gametest-pricing-2026-09-03.log`. The bounded dedicated server smoke reached `FutureShops server starting` with no exception, error, fatal, or economy failure before the deliberate timeout exit 124 in `/tmp/futureshops-runserver-pricing-2026-09-03.log`.

The rebuilt unpublished jar from `38bed89` passes `unzip -tq` and contains 397 entries.

SHA 256: `192087ee4037b7c7904036fc9c4098f24cca89864e9fac4d37d0f6b462e7a5ea`

SHA 512: `dddcd789cc10af27fc629e0104bdda911398a22c07764350b1f1c3d68ef0c603db91023392af16923497aa3b49c4b0ef57ffdc3726463b8a2ac19c80dc5b2522`

Shop placement limit persistence now has a versioned bounded decoder. Newer schemas, wrong containers, malformed UUID or value types, oversized limits, and invalid entries invalidate the complete limit map instead of silently accepting partial controls. Null players, negative counts, and over-limit writes fail closed. `ShopLimitsSavedDataTest` covers round trips, wrong containers, malformed entries, newer schemas, and invalid arguments.

The source commit is `5892499`. The rebuilt unpublished jar passes `unzip -tq` and contains 397 entries.

SHA 256: `5bf9f846cbb7b10f4728ec0249bf1ba6273f17baeab770db5b68b643024bc00a`

SHA 512: `cca5633b26129d7d4036ae8a1c39b817fe2648fae4dd2b5f5f4a0920747f0b209842a80e44a709e2c7eca381bda681abbac2d536e1d56222538ba9d5cd5b67d2`

The complete `test` task, `build` task, required GameTests, and dedicated server startup smoke passed after this change. The GameTest capture is `/tmp/futureshops-gametest-shop-limits-2026-09-03.log`, with both tests passing and a clean stop. The bounded server capture is `/tmp/futureshops-runserver-shop-limits-2026-09-03.log`; it reached `FutureShops server starting` with no exception, error, fatal, or economy failure before the deliberate timeout exit 124.

The GameTest suite now covers an interrupted player shop sale escrow on an unclean save. It verifies that exact item custody, checksums, the invalid clean marker, incomplete record, and explicit `RECOVERY_REQUIRED` state survive reconstruction without treating the prepared record as safe to retry.

The source commit is `61ca6c1`. `bash ./gradlew test --no-daemon`, `timeout 120s bash ./gradlew runGameTestServer --no-daemon`, and `bash ./gradlew build --no-daemon` all passed. The GameTest run executed three required tests and stopped cleanly in `/tmp/futureshops-gametest-unclean-sale-61ca6c1.log`. The rebuilt unpublished jar passes `unzip -tq` and contains 397 entries.

SHA 256: `f364c9dfdb63abb087d3d63922500b4e8e451f544807632f197c7d7f245bc174`

SHA 512: `5dce205c031a95bbbfcf0f8cc2fadf5bb817b34ea6c6f6de785dd9988152b95521d99cd4c7e5a4cde7748faf9b79dd559fc8d6f163a1ade6077266ac57021394`

The bounded dedicated server smoke reached `FutureShops server starting` with no exception, error, fatal, or economy failure before the deliberate timeout exit 124 in `/tmp/futureshops-runserver-unclean-sale-2626625.log`. The complete player shop crash and restart matrix, client and multiplayer evidence, full phase audit, and integration gates remain required before Phase 001 integration.

Player shop settlement recording now rejects null, blank, oversized, or zero quantity sale facts before changing pending proceeds or revenue history. Runtime owner limits also refuse new owners after the bounded persistence cap. The focused settlement suite, complete `test` task, required three test GameTest run, `build` task, and dedicated server startup smoke passed. The source commit is `e6efb4f`.

The rebuilt unpublished jar passes `unzip -tq` and contains 397 entries.

SHA 256: `092e248dff41be507424439bd9d8aaa63b92eca5ae330d7e9683f02409d6d25d`

SHA 512: `10a7fb54b2c00e21ac83dcb1d4341201c42ad26f1c6b61d13c7eb5f4b8f7a3c3f9c4c6130edae46bfba8c1d1ca87b3158034568eea883ca7e601cfd5e1c6a893`

The GameTest capture is `/tmp/futureshops-gametest-settlement-input-2026-09-03.log`. The bounded server capture is `/tmp/futureshops-runserver-settlement-input-2026-09-03.log`; it reached `FutureShops server starting` with no exception, error, fatal, or economy failure before the deliberate timeout exit 124.

Marketplace analytics now uses checked arithmetic for shop stock, settlement revenue, pending proceeds, listing counts, and supply counts. Product popularity aggregation uses a wide checked quantity accumulator, preventing valid bounded transaction history from wrapping an `int` before display. Settlement lifetime aggregation rejects overflow instead of returning a wrapped owner total. `MarketplaceAnalyticsArithmeticSourceTest` and `PlayerShopSettlementSavedDataPagingTest` cover the source contract and overflow refusal.

The source commit is `f040d23`. `bash ./gradlew test --no-daemon` and `bash ./gradlew build --no-daemon` passed. The required GameTest run executed three tests and stopped cleanly in `/tmp/futureshops-gametest-analytics-2026-09-03.log`. The bounded dedicated server smoke reached `FutureShops server starting` with no exception, error, fatal, or economy failure before the deliberate timeout exit 124 in `/tmp/futureshops-runserver-analytics-2026-09-03.log`.

The rebuilt unpublished jar passes `unzip -tq` and contains 397 entries.

SHA 256: `81f1317a567b0d4d0abca701841eee00ddcef17cb2e3ee76b710ab8a961f656f`

SHA 512: `f05204193bfdcbd3a8756c0319204273b78094e99e349b04437e69df2e4891cec35a4d328c5df9e37bb92cb8997199563bd18c773b0e6bc8c62fbee09cce06e5`

Minor unit command formatting now uses scaled `BigDecimal` output instead of floating point powers and manual absolute value arithmetic. This preserves exact trailing precision and formats `Long.MIN_VALUE` without absolute value overflow. `EconomyCommandUtilTest` covers the signed boundary and six decimal places.

The source commit is `9f07438`. The complete `test` task, `build` task, required three test GameTest run, and dedicated server startup smoke passed after this change. Captures are `/tmp/futureshops-gametest-format-9f07438.log` and `/tmp/futureshops-runserver-format-9f07438.log`.

The rebuilt unpublished jar passes `unzip -tq` and contains 397 entries.

SHA 256: `7b8ef0ff0186407f600216106e9f8b14811ec1638dfa084fc5f57f1af3ed64f2`

SHA 512: `a5bc2c2beea87d55e8da0c246ee7b915c7c19b330780d6cf1c4b6073dde29192fd8a0c1c467c2bd06cd97be8955e766cb8534ee4beb3095e37b91a75c9b0e30f`

The GameTest suite now also covers an interrupted player shop barter escrow after exact item removal and storage. It verifies that the checksummed payment custody, invalid clean marker, incomplete state, and explicit `RECOVERY_REQUIRED` transition survive reconstruction without retrying the stored payment as a new transaction. The source commit is `04b9cad`.

The focused barter escrow test and the complete GameTest server run passed. The four required tests completed and the server stopped cleanly in `/tmp/futureshops-gametest-barter-escrow-2026-09-03.log`.

Barter escrow preparation now rejects a null dimension instead of normalizing malformed identity data to an empty string. The focused saved-data test, complete `test` task, `build` task, and four-test GameTest run passed after source commit `e6f026a`. The rebuilt unpublished jar passes `unzip -tq` with 397 entries.

SHA 256: `38f9185539f089d4f947ec8c6dd150d705483f133661a25d8d1a89345bedf1d1`

SHA 512: `4c1cf5be3c35a50745b2907ea7c7fa3f805e0f883794fc64f3b97949ddf168feed4381dde8e01dbf7ad8a38497988e4a1a1b13b960cee59323174d3c728c4182`

The backup and restore runbook was rehearsed on a disposable local test world after source commit `449de6f`. The snapshot copied the complete `run/world`, `run/config`, and unpublished `build/libs/futureshops-2.3.0.jar` set. It contained 1,193 files and no external provider data because the fixture selected `internal`. A second disposable copy was restored and every file passed both SHA 256 and SHA 512 verification.

The rehearsal root was `/tmp/futureshops-backup-rehearsal.WEwmP3`. The SHA 256 manifest hash is `ce27acb972140a8f516597d6f1e3167d4f9fdd38c81039fae19bafef996d8e6e` and the SHA 512 manifest hash is `08cc9b0f7c20fe5463b1eddf17ae64e8475123af6dc619e9c3147a1934dc22fdedd05d6c3d1808a07d786fe60b1f577aebc11703139550ec2faf48bd797c8593`. The exact jar passed `unzip -tq` with SHA 256 `451e1d0075dccee62e1e9b0fb3ff0f37aec85d8a504c2d2c092d580c3dd1c0ba` and SHA 512 `a4233015ee22fdc7f3131f2f240038e0f157355d0e2b702955924b64c938bab7b817fd278b86e84bba075714dbfebbe0fa86d598d05a37837d1a9667512ac220`.

After source commit `cdeaf07`, the focused persistence tests, complete `test` task, and `build` task passed. The four required GameTests also passed and the GameTest server stopped cleanly in `/tmp/futureshops-gametest-missing-records-cdeaf07.log`. The new coverage proves that a current schema with a missing journal, custody, claims, or internal receipt collection becomes a read only integrity failure rather than an empty successful store.

The required startup smoke was rerun against the same build. The dedicated server reached `FutureShops server starting` without a FutureShops error in `/tmp/futureshops-runserver-missing-records-936203e.log`, and the client reached `FutureShops common setup complete` without a FutureShops error in `/tmp/futureshops-runclient-missing-records-936203e.log`. Both commands ended with the expected bounded timeout exit `124`; the client also emitted the existing GeckoLib Java 21 and Mixin class version warning.

Source commit `fd44f97` now rejects a player shop buyback counter overflow before provider preflight or item movement. The focused quantity safety test, complete `test` task, `build` task, and four required GameTests passed. The GameTest capture is `/tmp/futureshops-gametest-buyback-counter-2026-09-03.log`. The rebuilt unpublished jar passed `unzip -tq` with SHA 256 `57fde337577458f27710b82f9f3d99ae949d809d49bc7f583131f8bf76a6448b` and SHA 512 `8ca7b2cf8912f29a751a0eb0cb4dd0a13ea3efb98c7ff55c0d8190eefd80be646a02a1c4159176a17c6e1f457d29c6b3c3855b9b10a6a460467063ebe0190eaf`.

The required smoke checks were rerun against the same source and jar after `fd44f97`. The dedicated server reached `FutureShops server starting` without a FutureShops exception, error, fatal, or economy failure in `/tmp/futureshops-runserver-buyback-28702ac.log`. The client reached `FutureShops common setup complete` without a FutureShops exception, error, fatal, or economy failure in `/tmp/futureshops-runclient-buyback-28702ac.log`. Both bounded commands ended with the expected timeout exit `124`. The client retained the existing GeckoLib Java 21 and Mixin class version warning only.

The client cart state now bounds entries and quantities to the server transaction limits and saturates presentation totals instead of allowing integer or minor unit wraparound. The focused `PlayerShopCartStateTest`, serial complete `test` task, `build` task, and four required GameTests passed after the source change. The GameTest capture is `/tmp/futureshops-gametest-cart-overflow-b6195fa.log`.

The required client smoke reached `FutureShops common setup complete` without a FutureShops exception, error, fatal, or economy failure in `/tmp/futureshops-runclient-cart-overflow-b6195fa.log`. The bounded command ended with the expected timeout exit `124` and retained the existing GeckoLib Java 21 and Mixin class version warning only. The rebuilt unpublished jar passes `unzip -tq` with SHA 256 `7fe712b9e9da2eabad41329e6d971d0e7273cd69dbdcaf5abbea28686ffcf33a` and SHA 512 `2554fd22497d3b725cede678d11071096e155383ad4655cded7acb52ead0e1074e09d03f496e9828ddbfa08ea716ce30bc1317c7d726d8d024c38e38cd5d125b`.

The remaining client confirmation and barter preview arithmetic now uses saturating checked multiplication for money and item totals. The complete `test` task, `build` task, and required client smoke passed after this source change. The client smoke reached `FutureShops common setup complete` without a FutureShops exception, error, fatal, or economy failure in `/tmp/futureshops-runclient-ui-arithmetic-2026-09-03.log`; the bounded command ended with the expected timeout exit `124` and retained the existing GeckoLib Java 21 and Mixin class version warning only. The rebuilt unpublished jar passes `unzip -tq` with SHA 256 `ee3d623d0eddcd5645b1dd44379280c5f0392e344e25404a38e79d438fb4c1bb` and SHA 512 `7c1c036c576b9ef336f48766c767798343dc897b30b69f25c5e0dc521c844aa1cc2f9ff95c6fc05cd735aa568f56db9bfb3a109f4975f01d9139e1c971267cc0`.

Source commit `aa03243` now bounds the legacy client cart to 256 lines and 2304 units per line, rejects blank identifiers, saturates quantity aggregation, and uses checked saturating price multiplication and addition. Focused `ShopClientStateTest`, the complete `test` task, and `build` task passed. The bounded client startup smoke reached `FutureShops common setup complete` without a FutureShops exception, error, fatal, or economy failure in `run/logs/latest.log`; the command ended at the expected timeout boundary and retained only the existing GeckoLib Java 21 and Mixin class version warning. The rebuilt unpublished jar passes `unzip -tq` with SHA 256 `6fd0847f58597d90e32444538f53647b027bd7017c6525ca682f71ca853457e7` and SHA 512 `b7d22da1aa23ed4a8e9260b25dd9b1fc1f496012eb5f5d945b7c359d716aca83342f06a04ddf074cfb0604929f7b1eee5eade3d8eddad8eb3c8afb18180fc721`.
