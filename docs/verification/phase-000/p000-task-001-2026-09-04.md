# Phase 000 task 001 baseline refresh

## Scope and result

This packet records `P000-TASK-001` for the FutureShops 2.3.0 plan. It refreshes repository identity, platform pins, available Gradle tasks, source and test boundaries, economy callers, persistence owners, CodeGraph state, and the frozen issue evidence boundary. The task is complete for its local baseline outputs. It does not claim completion of Pixelmon acquisition, Pixelmon capability classification, Vault proof, runtime integration, or any later phase.

The exact current source revision is `4a88b444f75a639540eb2a9b62f0a300cc36b5d6`, on branch `envy/phase-002-pixelmon-vault`. The checkout was clean before this evidence refresh. The active saved goal, amended plan, manifest, and regenerated handoff were read without modification. No live issue 66 query, readback, or mutation was performed. The authoring evidence for issue 66 remains frozen for Phase 003.

## Repository identity and remote state

| Observation | Result |
| --- | --- |
| Repository root | `/mnt/hermes/projects/FutureShops` |
| Remote | `origin`, `https://github.com/MCEnvision/FutureShops.git` |
| Checkout branch | `envy/phase-002-pixelmon-vault` |
| Checkout commit | `4a88b444f75a639540eb2a9b62f0a300cc36b5d6` |
| Live remote default | `1.20.1` at `78fad4069d778996c24ecf5acc5cbe0e1edea7a2` |
| Live remote 1.21.1 branch | `cb1d252a2656cab0816b9469a15c8df5181323b8` |
| Local default branch | `1.20.1` at `78fad4069d778996c24ecf5acc5cbe0e1edea7a2` |
| Local default relationship | Local branch was safely fast forwarded to the live remote default because it was a strict ancestor. |
| Remote verification | `git fetch origin --prune` and `git ls-remote --symref origin HEAD refs/heads/1.20.1 refs/heads/1.21.1` |
| Source files | 242 tracked Java files under `src/main/java` |
| Test files | 58 tracked Java files under `src/test` |

The active phase branch remains separate from the default branch. No pull request or issue was opened or changed by this task. The repository root contains `.codegraph`, and the repository specific `AGENTS.md` is ignored and untracked as required.

## Plan and goal integrity

| Artifact | SHA 256 |
| --- | --- |
| `docs/plan/goal.md` | `4dcfb5f126ce39e6beb88fe12a1f8507c4529dc1f41d261f4f63dcbb6ebbc0e9` |
| `docs/general/plan.md` | `eb31dc0f992af73edd1cfc8155983ffcc30067f59a05e1f482c25635b5464cc4` |
| `docs/general/plan.index.json` | `a071ab073bba2af16bbafe6bee375552ffa63205c8f16973b1f7e6a052ddb8d3` |
| `docs/general/plan.handoff.json` | `573b7eba4458634c86bf174cd539fd2e301fd4bec95611aebd32abdedbe2bc7b` |

The goal names `CORE-PHASE-000`, `/mnt/hermes/projects/FutureShops/docs/general/phases/plan-phase-000.md`, and `P000-TASK-001` as the current checkpoint. The plan set covers mandatory requirements `CORE-REQ-001` through `CORE-REQ-022`, phases `CORE-PHASE-000` through `CORE-PHASE-003`, and the added `DEC-021` headless-first diagnostic contract. Optional future requirements remain excluded. The plan and goal were not refreshed or rebound.

## Platform metadata and command matrix

| Boundary | Verified value |
| --- | --- |
| Minecraft | `1.21.1` |
| Minecraft range | `[1.21.1,1.22)` |
| NeoForge | `21.1.248` |
| NeoForge range | `[21,)` |
| FutureShops version | `2.3.0` |
| Mod id | `futureshops` |
| Java toolchain | 21, Gradle selected `/usr/lib/jvm/java-21-openjdk-amd64` version `21.0.11` |
| Gradle wrapper | 8.8 |
| ModDevGradle | 2.0.141 |
| Parchment | `2024.11.17` |
| GeckoLib | NeoForge `1.21.1:4.8.4` |
| JUnit | Jupiter `5.10.2` |

`bash ./gradlew tasks --all --no-daemon` passed. The command inventory includes `test`, `check`, `build`, `jar`, `compileJava`, `compileTestJava`, `javadoc`, `runClient`, `runServer`, `runGameTestServer`, and `runData`. The repository has no formatter or static analysis task.

The metadata files used by this baseline have these SHA 256 values.

| File | SHA 256 |
| --- | --- |
| `gradle.properties` | `d1d1e9ff9ef57c1610e0e2e732397897984627f4e7bfe62bb17796c7fc932e2` |
| `settings.gradle` | `b94fb4013af72a91c29e63a80c816b56a26abe360c24bcc50e4cc3c602b3ef92` |
| `build.gradle` | `8c17a5355f3f01df5a835991a8f8c8440f4043f872f9e8eb30e2c2d40cb889d6` |
| `src/main/templates/META-INF/neoforge.mods.toml` | `b42f12b4d10756297647ea5657ba6ab36803ede80e84637fc470d9c4ed052ff8` |

## Disposable baseline verification

A source archive of the exact checkout was extracted to `/tmp/futureshops-p000-task001-current.xHFpWe`. The following commands passed there.

```text
timeout 300 bash ./gradlew tasks --all --no-daemon
timeout 300 bash ./gradlew test --no-daemon
timeout 300 bash ./gradlew build --no-daemon
```

The initial scripted attempt passed Gradle's `--all` option to `test` and was rejected before test execution. The corrected invocations above passed. The test run completed successfully with 10 actionable tasks. The build completed successfully with 11 tasks and produced `build/libs/futureshops-2.3.0.jar` in the disposable workspace. The jar has SHA 256 `f501ad192ed5e79c4d17dce524fe6d6a95ede42e8f1047b48be531b65a52f7f6` and SHA 512 `367b053ced95bbb6c7f4486fb39a5681c5295458705587f7f4d7479ec233818e8cd3c50890475543554796c812869a8a4d1bc679ef85436a977a75355bef239a`. `unzip -tq` passed. The rendered metadata identifies mod `futureshops`, version `2.3.0`, Minecraft range `[1.21.1,1.22)`, and NeoForge range `[21,)`.

The disposable workspace was removed after the jar hash and metadata were recorded and its absence was verified. No external jar, world, log, or generated build output was copied into the repository. No owned process remained from this baseline run.

## CodeGraph and source ownership

CodeGraph status was up to date at the time of capture.

| Index value | Result |
| --- | --- |
| Files | 302 |
| Nodes | 7,081 |
| Edges | 17,277 |
| Backend | node sqlite with full WAL |

The economy boundary is centered on `BalanceManager`, `EconomyProvider`, `EconomyTransactionCoordinator`, and `InternalEconomyProvider`. `BalanceManager` owns provider selection and lifecycle state. The coordinated provider and transaction coordinator are the required mutation route. The Pixelmon registration is optional and isolated under `compat/pixelmon`; no Vault implementation is present in the standard source set.

The complete current `BalanceManager` caller set is:

* lifecycle, `Futureshops.java`
* public API, `api/ShopModAPI.java`
* commands, `BalTopCommand.java`, `BalanceCommand.java`, `DepositCommand.java`, `EconomyCommandUtil.java`, `PayCommand.java`, `ShopAdminCommand.java`, and `WithdrawCommand.java`
* physical currency, `money/MoneyItem.java`
* shop and analytics services, `server/shop/MarketplaceAnalyticsService.java`, `PlayerShopBlockService.java`, and `ShopDataService.java`
* transactions, `server/transaction/ShopBuyService.java` and `ShopSellService.java`
* regression coverage, `gametest/EconomyGameTests.java`

The provider declaration and resolution surface is `api/economy/EconomyProvider.java`, `EconomyProviderContext.java`, `EconomyProviderFactory.java`, `EconomyProviderRegistry.java`, `ProviderRegistration.java`, and `ProviderResolution.java`. Server implementations and lifecycle are `server/economy/BalanceManager.java`, `CoordinatedEconomyProvider.java`, `EconomyTransactionCoordinator.java`, `ExternalLegacyEconomyProvider.java`, `InternalEconomyProvider.java`, `PublicInternalEconomyProvider.java`, `TransactionResult.java`, and `UnavailableEconomyProvider.java`. Pixelmon integration files are `compat/pixelmon/PixelmonEconomyProvider.java` and `PixelmonEconomyProviderRegistration.java`.

Persistence ownership is split between `InternalBalanceSavedData`, `InternalEconomyReceiptSavedData`, `EconomyJournalSavedData`, `EconomyCustodySavedData`, `EconomyClaimSavedData`, and their stores and journals under `server/economy`. Shop, escrow, settlement, pricing, registry, and transaction history saved data remain owned by their corresponding `server/shop`, `server/pricing`, and `server/transaction` services. `SavedDataMigrations.java` owns persistence schema migration dispatch. The current source already exposes receipt, journal, custody, and claim boundaries, but this task does not claim that a Pixelmon or Vault external mutation is safe.

## Drift and next gate

The prior baseline packet is historical and records older 2.2.x metadata. This refresh establishes the current 2.3.0 and NeoForge 21.1.248 source state, the amended plan provenance, and fresh disposable test and build evidence. No unrelated platform pin was changed in this task. The next unfinished mandatory task is `P000-TASK-002`, exact Pixelmon artifact and development access intake. It must use the exact read only Pixelmon 9.4.0 jar and authorized inspection and compile probe rules. Future tasks own Pixelmon capability classification, the native `PlayerPartyStorage` mixin, the separate Vault bridge or backend proof, and the DEC-021 diagnostic implementation.
