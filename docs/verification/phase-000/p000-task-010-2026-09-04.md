# P000-TASK-010 Verification, 2026-09-04

## Scope

This record covers the current `CORE-PHASE-000` fixture, platform, API, selection, build, and standard server verification for `CORE-REQ-001` through `CORE-REQ-003`.

The verified source revision is `bf1f74dac6a96feed654201bd1403381076ed872`. No source, test, build, dependency, or resource files changed between the artifact evidence recorded at `f1b6de2ad3a7dd554a59cc6acc283ba8a9bf269d` and this revision. The intervening commits contain documentation evidence only.

## Deterministic fixture coverage

The focused fixture suites completed in unique disposable workspaces and were removed after their final consumer.

| Area | Evidence | Result |
| --- | --- | --- |
| API contract, registry, and coordinator | `p000-task-006-2026-09-04.md` | Passed |
| Deterministic registry validation | `p000-task-007-2026-09-04.md` | Passed |
| Restart-only selection and recovery states | `p000-task-008-2026-09-04.md` | Passed |
| Capability deficient provider refusal | `p000-task-003-2026-09-04.md` | Passed |

The fixtures cover current, incompatible, throwing, ambiguous, capability-deficient, duplicate, malformed, reserved, and late provider cases. They verify that missing receipt and retry capabilities reject before transaction intent, custody, or provider mutation, and that failed external selection does not fall back to `internal`.

## Build and data verification

The following commands completed successfully in disposable workspace `/tmp/futureshops-p000-task010.5TGtrB`.

```text
bash ./gradlew runData --no-daemon --console=plain
bash ./gradlew test --no-daemon --console=plain
bash ./gradlew runGameTestServer --no-daemon --console=plain
bash ./gradlew build --no-daemon --console=plain
```

`runData`, `test`, and `build` reported `BUILD SUCCESSFUL`. The GameTest run reported all 16 required tests passed. The workspace and generated test output were removed after the checks completed.

The same-byte artifact recorded by the preceding baseline task is the current source artifact because no implementation inputs changed after that build.

```text
sha256  f501ad192ed5e79c4d17dce524fe6d6a95ede42e8f1047b48be531b65a52f7f6
sha512  367b053ced95bbb6c7f4486fb39a5681c5295458705587f7f4d7479ec233818e8cd3c50890475543554796c812869a8a4d1bc679ef85436a977a75355bef239a
```

## Dedicated server smoke

A first smoke attempt in the disposable workspace failed because the default server port was already in use. Its temporary workspace and crash output were removed. This was an environment collision, not a mod startup or linkage failure.

A retry used `/tmp/futureshops-p000-task010-server2.FEmgpf` with an isolated server configuration, `server-port=25577`, `server-ip=127.0.0.1`, `online-mode=false`, and `eula=true`. The latest log showed:

```text
FutureShops 2.3.0 (futureshops)
FutureShops common setup complete.
Done (4.011s)! For help, type "help"
FutureShops server starting.
FutureShops server stopping.
Stopping server
```

The server smoke reached readiness and shut down cleanly under the bounded timeout. The sanitized latest log had SHA-256 `3db5384103598d42febb497d196b896b430932433eba57e42d471926d0269ab4` and SHA-512 `e4ae11fbd0fe39ab95e100a9c3c594a665f256dc001c7af8a599e651b9962a301428589876a5cfe1866911a0a8a9224e4ffeeeada3fcb15d7f0fe2baa1df62c7`. The exact workspace, logs, world, and generated configuration were removed and process absence was verified.

## Client smoke disposition

Graphical client acceptance is not claimed here. The current execution host is headless `node-1`, and no authorized laptop desktop connection was available in this run. The host policy prohibits substituting Xvfb, a virtual display, software rendering, or a headless client for the required laptop client. The client gate remains explicitly unverified and must be completed on the authorized laptop with the same jar bytes before phase completion.

## Isolation and external bytes

The Pixelmon, Vault, bridge, and hybrid artifacts remained outside the repository and were not rebuilt, modified, bundled, or repackaged. The standard smoke used the FutureShops artifact without optional Pixelmon or Vault classes. Disposable runtimes and all test-created output were removed after verification.

## Gate status

The deterministic API, registry, selection, data, GameTest, build, and dedicated server checks pass. The graphical client check is the remaining P000-TASK-010 verification gate. This record does not close the task or claim full phase completion.
